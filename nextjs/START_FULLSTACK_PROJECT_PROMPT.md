
Действуй как профессиональный промпт-инженер, информационный архитектор, Senior Fullstack-разработчик (Next.js, TypeScript, Supabase), UX/UI-эксперт и маркетолог.

Я разрабатываю новый проект с использованием Cursor AI агентов и MCP-сервера context7 для доступа к актуальной документации.  
Технический стек, требования к безопасности, производительности и дизайну остаются **неизменными** для всех проектов этого типа.  
Единственное, что меняется от проекта к проекту — это:

1. **Краткое описание функционала** (ниша, цели, пользовательские сценарии).
2. **Уникальные учетные данные проекта (creds)**: Supabase, Auth.js, Storage и т.п.

---

### 🔧 НЕИЗМЕННЫЕ КОНСТАНТЫ ПРОЕКТА

**Технический стек:**
- Next.js v16 с App Router
- TypeScript
- Prisma ORM
- Zustand (для локального состояния)
- React Query (при необходимости)
- React Compiler включен
- Auth.js (NextAuth v5) с JWT-токенами
- Supabase: PostgreSQL (через pooled-подключение на порту 6543), Storage для медиа
- UI: **shadcn/ui** — все компоненты устанавливаются при первичной настройке командой:  
  ```bash
  npx shadcn-ui@latest add --all
  ```
- Дополнительно для эффектов и анимаций на лендингах — **Aceternity UI**
- WYSIWYG: BlockNote
- Архитектурный подход: проект из `examples\electrician` — используй его как архитектурный референс (но не копируй функционал!)
- Конфигурация prisma должна содержаться в prisma.config.ts
- CRUD-операции реализуются через **server actions**, а не через API routes
- Миграции БД — через Prisma CLI с использованием **direct-подключения** (`DIRECT_URL`)

**Безопасность и инфраструктура:**
- Используется единое pooled-подключение к БД (`DATABASE_URL` через PgBouncer)
- Prisma `$transaction` — только там, где требуется атомарность (например, создание заказа)
- Все учетные данные хранятся в `docs/secure_data.md` и `.env.local`
- Интеграционные тесты **не создаются** на данном этапе

**UX/UI и маркетинговые требования:**
- Сайт должен выглядеть как «миллион долларов»: визуально эффектный, высококонверсионный, с WOW-эффектом
- **Мобильная версия — приоритет №1**. Все компоненты должны быть mobile-first
- Hero-секция на homepage — без фиксированного контента; оставь её гибкой для последующей визуальной реализации с использованием анимаций (Aceternity UI)

**Core Web Vitals** применяются **только к публичным страницам** и должны соответствовать:
- **LCP** < 2.5s  
- **FID** < 100ms  
- **CLS** < 0.1  
- **TTFB** < 600ms  

**Админка:**
- Left adaptive sidebar
- Доступ к админке, ролевая модель и требования к логированию CRUD-операций — **определяются в описании функционала** для каждого проекта отдельно и **не выносятся в boilerplate**

**Язык и локализация:**
- Только русский язык
- Интернационализация не предполагается в ближайшем будущем

---

### 📥 ПОДСТАВЛЯЕМЫЕ ДАННЫЕ

#### 1. Описание функционала проекта:
<!-- НАЧАЛО ОПИСАНИЯ ФУНКЦИОНАЛА -->
{{PROJECT_FUNCTIONALITY_DESCRIPTION}}
<!-- КОНЕЦ ОПИСАНИЯ ФУНКЦИОНАЛА -->

#### 2. Учетные данные (creds):
Создай файл `.env.local` со следующей структурой:

```env
# Storage (Supabase)
NEXT_PUBLIC_SUPABASE_BUCKET_NAME={{BUCKET_NAME}}
NEXT_PUBLIC_SUPABASE_STORAGE_ENDPOINT={{STORAGE_ENDPOINT}}
NEXT_PUBLIC_SUPABASE_REGION={{REGION}}

# Auth.js
AUTH_SECRET={{AUTH_SECRET}}

# Supabase API
NEXT_PUBLIC_SUPABASE_URL={{PROJECT_URL}}
NEXT_PUBLIC_SUPABASE_ANON_KEY={{API_KEY}}

# Database
DATABASE_URL={{DATABASE_URL_POOLED}}   # через PgBouncer, порт 6543
DIRECT_URL={{DATABASE_URL_DIRECT}}     # для миграций
```

Также создай `docs/secure_data.md` с пояснением каждого поля и его значением.

---

### 📝 ТВОИ ЗАДАЧИ

1. **Создай `docs/app_functionality.md`** — детализированное, структурированное описание функционала проекта на основе предоставленного описания. Раздели на:
   - Public Website pages
   - Dashboard / Admin pages
   - Пользовательские сценарии
   - Бизнес-логика (заказы, статусы, аналитика и т.д.)
   - Требования к UX/UI, безопасности и производительности

2. **Создай `docs/documentation_implementation_plan.md`** — поэтапный план написания всей необходимой документации проекта, оптимизированный для выполнения Cursor AI агентами. План должен включать:
   - Фазы (например: Анализ → Архитектура → Дизайн → Логика → Безопасность → Аналитика)
   - Цель каждой фазы
   - Список документов/артефактов, создаваемых на фазе
   - Зависимости между фазами
   - Рекомендуемый порядок выполнения агентами

3. Обеспечь полную согласованность между `app_functionality.md` и `documentation_implementation_plan.md`.

**Вывод должен быть на русском языке. Не генерируй код — только документацию.**
```
