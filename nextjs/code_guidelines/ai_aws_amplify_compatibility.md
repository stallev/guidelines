# Moved: AWS Amplify compatibility (historical)

**This file name is kept for old links.** Runtime hosting guidance for this project now lives here:

- Runtime guidance should follow the active runtime policy from this directory index (`README.md`).

**Status:** historical note (legacy context only).

The product is deployed on **Vercel**, not AWS Amplify. Do not apply Amplify-specific constraints (streaming, on-demand ISR, 220 MB build cap, etc.) to `apps/web` unless you are explicitly working with archived material.
