# Project Implementation Plan: Трансформация статического сайта в WordPress тему

## Обзор проекта

**Цель**: Трансформация статического HTML/CSS/JS сайта в динамическую WordPress тему с возможностью редактирования всего контента через плагин Carbon Fields.

**⚠️ ВАЖНО: Данный документ предназначен для использования в качестве руководства AI Agent при трансформации статических одностраничных и многостраничных сайтов в динамические WordPress темы. Все требования и правила, описанные в этом документе, должны строго соблюдаться.**

**🎯 Назначение документа:**
Этот документ является **полным универсальным руководством** для AI Agent, который будет трансформировать статические HTML/CSS/JS сайты в динамические WordPress темы с использованием Carbon Fields.

**🔧 Универсальность:**
- Все примеры кода используют переменные-плейсхолдеры (`{theme_slug}`, `{THEME_NAME}`, и т.д.)
- Все конкретные данные заменены на указания брать их из статического сайта
- Документ не привязан к конкретному проекту и может использоваться для любого сайта
- **ОБЯЗАТЕЛЬНО** замените все плейсхолдеры на конкретные значения перед использованием 

**Основные принципы:**
1. **ВСЕ данные** берутся из статического сайта (каталог `static_website/`)
2. **ВСЕ страницы** редактируются через Carbon Fields (включая Privacy)
3. **Строгая структура** каталогов (`template-pages/`, `template-parts/[type]/`, `template-parts/shared/`)
4. **Rich text поля** требуют `wp_kses_post(nl2br(...))` при выводе
5. **Complex Fields** требуют `set_collapsed(false)` для начальных данных
6. **Все значения** `set_default_value()` берутся из исходного HTML
7. **⚠️ ОБЯЗАТЕЛЬНО:** Все взаимодействия с Carbon Fields выполняются через helper-функции (`{theme_slug}_theme_option()`, `{theme_slug}_post_meta()`), а не напрямую через `carbon_get_theme_option()` и `carbon_get_post_meta()`
8. **Helper-функции** обеспечивают безопасность, единообразие, fallback значения и упрощают поддержку кода

**🔄 ОБНОВЛЕНО ПОСЛЕ РЕФАКТОРИНГА:**
Данный документ был обновлен с учетом рефакторинга кодовой базы темы. Все примеры кода теперь отражают:
- Использование helper-функций из `inc/helpers.php` для работы с Carbon Fields и ассетами
- Новую структуру модулей в `inc/` (helpers.php, template-tags.php, seo-functions.php и т.д.)
- Улучшенную систему управления ассетами с автоматическим cache-busting через `filemtime()`
- Использование `wp_script_add_data()` для добавления defer атрибутов
- Улучшенные функции поиска шаблонов из подкаталога `template-pages/`

**Стек технологий**:
- WordPress
- Carbon Fields 3.x
- PHP 7.4+
- Существующая верстка (HTML5, CSS3, JavaScript ES6+)

**🔧 Переменные-плейсхолдеры:**

Данный документ использует переменные-плейсхолдеры для универсальности. **ОБЯЗАТЕЛЬНО** замените их на конкретные значения для вашего проекта:

| Переменная | Описание | Пример замены |
|-----------|----------|---------------|
| `{theme_slug}` | Slug темы (строчными буквами, без пробелов) | `mytheme`, `business-site` |
| `{THEME_NAME}` | Полное название темы | `My Theme`, `Business Theme` |
| `{THEME_URI}` | URL сайта темы | `https://example.com` |
| `{AUTHOR_NAME}` | Имя автора темы | `John Doe` |
| `{THEME_DESCRIPTION}` | Описание темы | `Профессиональная тема...` |
| `{Заголовок...}` | Конкретный контент из статического сайта | Берется из HTML |

**⚠️ ВАЖНО:** Используйте **одинаковый** `{theme_slug}` во всех местах одного проекта!

---

## Фаза 1: Подготовка и структура темы

### 1.1 Создание базовых файлов темы WordPress

**Задачи**:

1. **Создать `style.css`** с метаинформацией темы:
```css
/*
Theme Name: {THEME_NAME}
Theme URI: {THEME_URI}
Author: {AUTHOR_NAME}
Description: {THEME_DESCRIPTION}
Version: 1.0.0
Requires at least: 5.9
Tested up to: 6.4
Requires PHP: 7.4
License: GNU General Public License v2 or later
Text Domain: {theme_slug}
*/
```

**Примечание:** Замените все переменные-плейсхолдеры на конкретные значения для вашего проекта. См. раздел 8.1 для полного списка переменных.

2. **Создать базовые файлы темы**:
   - `functions.php` - основной файл функций
   - `header.php` - шапка сайта
   - `footer.php` - подвал сайта
   - `index.php` - главная страница
   - `page.php` - шаблон страницы по умолчанию
   - `screenshot.png` - скриншот темы для админки

**⚠️ ВАЖНО:** Все шаблоны страниц (Template Name) должны находиться в каталоге `template-pages/`, а НЕ в корне темы!

3. **Создать структуру папок**:
```
wp-content/themes/{theme_slug}/
├── assets/
│   ├── css/
│   ├── fonts/
│   ├── images/
│   ├── icons/
│   └── js/
├── inc/
│   ├── helpers.php                  # Helper-функции для работы с Carbon Fields и ассетами
│   ├── template-tags.php            # Функции для шаблонов (fallback меню и т.д.)
│   ├── theme-setup.php              # Базовая настройка темы
│   ├── enqueue-scripts.php          # Подключение стилей и скриптов
│   ├── seo-functions.php            # SEO функции (Schema.org, OG теги)
│   ├── form-helpers.php             # Санитизация и валидация форм
│   ├── telegram-handler.php         # Отправка уведомлений в Telegram
│   ├── ajax-handlers.php            # AJAX обработчики форм
│   └── carbon-fields-config.php     # Конфигурация Carbon Fields
├── template-pages/          # Шаблоны страниц WordPress
│   ├── page-home.php        # Шаблон главной страницы
│   ├── page-privacy.php     # Шаблон страницы Privacy Policy
│   └── ...                  # Другие шаблоны страниц
├── template-parts/          # Части шаблонов (секции)
│   ├── home/                # Секции главной страницы
│   │   ├── hero-section.php
│   │   ├── specifications-section.php
│   │   ├── about-section.php
│   │   ├── services-section.php
│   │   ├── advantages-section.php
│   │   ├── how-it-works-section.php
│   │   ├── testimonials-section.php
│   │   └── contacts-section.php
│   ├── privacy/             # Секции страницы Privacy (если нужны)
│   │   └── ...
│   └── shared/               # Общие секции (используются на разных страницах)
│       └── ...
├── static_website/ (существующие файлы)
├── specs/
└── README.md
```

**⚠️ ОБЯЗАТЕЛЬНЫЕ ТРЕБОВАНИЯ К СТРУКТУРЕ:**

1. **Каталог `template-pages/`** - ВСЕ шаблоны страниц WordPress (Template Name) должны находиться здесь:
   - `template-pages/page-home.php`
   - `template-pages/page-privacy.php`
   - `template-pages/page-*.php` для других страниц
   - НЕ создавать шаблоны в корне темы!

2. **Каталог `template-parts/`** - ДОЛЖЕН быть организован по подкаталогам:
   - `template-parts/home/` - секции главной страницы
   - `template-parts/privacy/` - секции страницы Privacy (если нужны)
   - `template-parts/[page-type]/` - секции для каждого типа страницы
   - `template-parts/shared/` - общие секции (используемые на разных страницах)
   - НЕ хранить файлы секций напрямую в `template-parts/`!

3. **Использование в шаблонах:**
```php
// ✅ ПРАВИЛЬНО
get_template_part('template-parts/home/hero', 'section');
get_template_part('template-parts/shared/breadcrumbs');

// ❌ НЕПРАВИЛЬНО
get_template_part('template-parts/hero', 'section');  // без подкаталога
```

### 1.2 Копирование и организация ассетов

**Задачи**:

1. **Скопировать из `static_website/assets/` в `assets/`**:
   - `css/styles.css` → `assets/css/main.css`
   - `fonts/` → `assets/fonts/`
   - `images/` → `assets/images/`
   - `icons/` → `assets/icons/`
   - `js/script.js` → `assets/js/main.js`

2. **Скопировать favicon и мета файлы** в корень темы:
   - `favicon.ico`, `favicon.svg`, `favicon-96x96.png`
   - `apple-touch-icon.png`
   - `site.webmanifest`

---

## Фаза 2: Настройка WordPress функций

### 2.1 Базовая настройка темы (inc/theme-setup.php)

**Задачи**:

1. **Регистрация поддержки возможностей WordPress**:
   - `add_theme_support('title-tag')`
   - `add_theme_support('post-thumbnails')`
   - `add_theme_support('custom-logo')`
   - `add_theme_support('html5')`
   - `register_nav_menus()` для меню

2. **Настройка размеров изображений**:
```php
add_image_size('hero-image', 1920, 1080, true);
add_image_size('service-image', 600, 400, true);
add_image_size('testimonial-avatar', 80, 80, true);
```

3. **Регистрация областей виджетов** (если потребуется):
```php
register_sidebar(array(
    'name' => 'Footer Widget Area',
    'id' => 'footer-widget',
    // ...
));
```

4. **⚠️ ОБЯЗАТЕЛЬНО: Отключение RSS ленты**:

   RSS лента должна быть полностью отключена для всех тем. Это требование безопасности и оптимизации.
   
   ```php
   /**
    * Отключение RSS ленты
    */
   function {theme_slug}_disable_rss_feeds() {
       // Удаляем ссылки на RSS из head
       remove_action('wp_head', 'feed_links', 2);
       remove_action('wp_head', 'feed_links_extra', 3);
       
       // Редиректим все RSS запросы на главную страницу
       add_action('do_feed', '{theme_slug}_disable_feed', 1);
       add_action('do_feed_rdf', '{theme_slug}_disable_feed', 1);
       add_action('do_feed_rss', '{theme_slug}_disable_feed', 1);
       add_action('do_feed_rss2', '{theme_slug}_disable_feed', 1);
       add_action('do_feed_atom', '{theme_slug}_disable_feed', 1);
       add_action('do_feed_rss2_comments', '{theme_slug}_disable_feed', 1);
       add_action('do_feed_atom_comments', '{theme_slug}_disable_feed', 1);
   }
   
   /**
    * Редирект RSS feed на главную страницу
    */
   function {theme_slug}_disable_feed() {
       wp_redirect(home_url(), 301);
       exit;
   }
   
   add_action('init', '{theme_slug}_disable_rss_feeds');
   ```
   
   **Почему это важно:**
   - Удаляет ссылки на RSS из `<head>` (SEO оптимизация)
   - Редиректит все RSS запросы на главную страницу с кодом 301 (безопасность)
   - Предотвращает доступ к RSS лентам через URL
   - Улучшает производительность (меньше запросов к базе данных)

5. **⚠️ ОБЯЗАТЕЛЬНО: Перенаправление страниц авторов на 404**:

   Все страницы авторов (`/author/username/`) должны автоматически перенаправляться на страницу 404. Это требование безопасности и SEO.
   
   ```php
   /**
    * Перенаправление страниц авторов на 404
    */
   add_action('template_redirect', '{theme_slug}_disable_author_pages');
   function {theme_slug}_disable_author_pages() {
       if (is_author()) {
           global $wp_query;
           $wp_query->set_404();
           status_header(404);
           nocache_headers();
       }
   }
   ```
   
   **Почему это важно:**
   - Предотвращает доступ к страницам авторов (безопасность)
   - Избегает дублирования контента (SEO)
   - Скрывает информацию о пользователях сайта
   - Улучшает UX (пользователи видят понятную страницу 404 вместо пустой страницы автора)

### 2.2 Подключение стилей и скриптов (inc/enqueue-scripts.php)

**Задачи**:

1. **Правильное подключение CSS**:
```php
wp_enqueue_style('{theme_slug}-fonts', get_template_directory_uri() . '/assets/fonts/fonts.css', array(), '1.0.0');
wp_enqueue_style('{theme_slug}-main', get_template_directory_uri() . '/assets/css/main.css', array('{theme_slug}-fonts'), '1.0.0');
```

2. **Подключение JavaScript**:
```php
wp_enqueue_script('{theme_slug}-main', get_template_directory_uri() . '/assets/js/main.js', array(), '1.0.0', true);
```

3. **Локализация скриптов для AJAX**:
```php
wp_localize_script('{theme_slug}-main', '{theme_slug}Data', array(
    'ajaxUrl' => admin_url('admin-ajax.php'),
    'nonce' => wp_create_nonce('{theme_slug}-nonce'),
    'siteUrl' => home_url('/'),
));
```

4. **Подключение Google Analytics** через wp_head:
```php
add_action('wp_head', '{theme_slug}_google_analytics');
```

**⚠️ ОБНОВЛЕНО ПОСЛЕ РЕФАКТОРИНГА:** Раздел 2.2-2.5 переработан. Теперь используется система helper-функций для работы с ассетами и Carbon Fields. См. обновленные разделы ниже.

### 2.2 Создание helper-функций (inc/helpers.php)

**⚠️ КРИТИЧЕСКОЕ ТРЕБОВАНИЕ:** Все взаимодействия с Carbon Fields должны выполняться через helper-функции. Это обеспечивает безопасность, единообразие и упрощает поддержку кода.

**Основные функции (см. полный код в актуальной кодовой базе темы):**
- `{theme_slug}_is_carbon_active()` - проверка доступности Carbon Fields
- `{theme_slug}_theme_option($key, $default)` - безопасное получение опций темы
- `{theme_slug}_post_meta($post_id, $key, $default)` - безопасное получение мета-полей
- `{theme_slug}_normalize_phone($phone)` - нормализация телефонов
- `{theme_slug}_build_tel_href($phone, $fallback)` - создание tel: ссылок
- `{theme_slug}_asset_uri($relative_path)` - получение URI ассетов
- `{theme_slug}_asset_version($relative_path)` - версия ассета для cache-busting (на основе filemtime)
- `{theme_slug}_get_svg_allowed_tags()` - возвращает массив разрешенных тегов и атрибутов для безопасного вывода SVG через `wp_kses()`

### 2.3 Подключение стилей и скриптов (inc/enqueue-scripts.php)

**ОБНОВЛЕНО:** Теперь используется система helper-функций:

```php
function {theme_slug}_enqueue_scripts() {
    // Используем helper-функции для URI и версий
    wp_enqueue_style(
        '{theme_slug}-fonts',
        {theme_slug}_asset_uri('fonts/fonts.css'),
        array(),
        {theme_slug}_asset_version('fonts/fonts.css')
    );
    
    wp_enqueue_style(
        '{theme_slug}-main',
        {theme_slug}_asset_uri('css/main.css'),
        array('{theme_slug}-fonts'),
        {theme_slug}_asset_version('css/main.css')
    );
    
    wp_enqueue_script(
        '{theme_slug}-main',
        {theme_slug}_asset_uri('js/main.js'),
        array(),
        {theme_slug}_asset_version('js/main.js'),
        true
    );
    
    // Добавляем defer через wp_script_add_data (более надежно)
    wp_script_add_data('{theme_slug}-main', 'defer', true);
    
    wp_localize_script('{theme_slug}-main', '{theme_slug}Data', array(
        'ajaxUrl' => admin_url('admin-ajax.php'),
        'nonce'   => wp_create_nonce('{theme_slug}-nonce'),
        'siteUrl' => home_url('/'),
    ));
}
```

**Resource hints:**
```php
function {theme_slug}_add_resource_hints() {
    foreach (array('fonts/inter/Inter-Regular.woff2', 'fonts/inter/Inter-Medium.woff2') as $font) {
        printf(
            '<link rel="preload" href="%s" as="font" type="font/woff2" crossorigin>%s',
            esc_url({theme_slug}_asset_uri($font)),
            "\n"
        );
    }
}
add_action('wp_head', '{theme_slug}_add_resource_hints', 1);
```

**Отключение ненужных стилей и скриптов WordPress:**
```php
/**
 * Отключение ненужных стилей и скриптов WordPress
 * Улучшает производительность и скрывает информацию о версии WordPress
 */
function {theme_slug}_disable_unnecessary_assets() {
    // Отключение стилей блоков Gutenberg (если не используются)
    wp_dequeue_style('wp-block-library');
    wp_dequeue_style('wp-block-library-theme');
    wp_dequeue_style('global-styles'); // Глобальные стили темы
    
    // Отключение стилей для классических блоков
    wp_dequeue_style('classic-theme-styles');
    
    // Отключение эмодзи скриптов и стилей
    remove_action('wp_head', 'print_emoji_detection_script', 7);
    remove_action('admin_print_scripts', 'print_emoji_detection_script');
    remove_action('wp_print_styles', 'print_emoji_styles');
    remove_action('admin_print_styles', 'print_emoji_styles');
    remove_filter('the_content_feed', 'wp_staticize_emoji');
    remove_filter('comment_text_rss', 'wp_staticize_emoji');
    remove_filter('wp_mail', 'wp_staticize_emoji_for_email');
    
    // Отключение DNS prefetch для эмодзи
    add_filter('emoji_svg_url', '__return_false');
}
add_action('wp_enqueue_scripts', '{theme_slug}_disable_unnecessary_assets', 100);

/**
 * Удаление inline стилей эмодзи из head
 * Используем output buffering на уровне всей страницы
 */
function {theme_slug}_remove_emoji_inline_styles_start() {
    ob_start();
}
add_action('template_redirect', '{theme_slug}_remove_emoji_inline_styles_start', 1);

function {theme_slug}_remove_emoji_inline_styles_end() {
    $buffer = ob_get_clean();
    // Удаляем inline стили эмодзи
    $buffer = preg_replace('#<style id="wp-emoji-styles-inline-css">.*?</style>#s', '', $buffer);
    echo $buffer;
}
add_action('shutdown', '{theme_slug}_remove_emoji_inline_styles_end', 999);

/**
 * Скрытие версии WordPress
 * Убирает мета-тег generator из head
 */
function {theme_slug}_remove_wp_version() {
    return '';
}
add_filter('the_generator', '{theme_slug}_remove_wp_version');

/**
 * Удаление версии WordPress из query string стилей и скриптов
 */
function {theme_slug}_remove_version_scripts_styles($src) {
    if (strpos($src, 'ver=')) {
        $src = remove_query_arg('ver', $src);
    }
    return $src;
}
add_filter('style_loader_src', '{theme_slug}_remove_version_scripts_styles', 9999);
add_filter('script_loader_src', '{theme_slug}_remove_version_scripts_styles', 9999);
```

**⚠️ ВАЖНО**: Эти функции должны быть добавлены в `inc/enqueue-scripts.php` для оптимизации производительности и безопасности сайта.

### 2.4 Поддержка шаблонов из подкаталога template-pages/

**⚠️ КРИТИЧЕСКОЕ ТРЕБОВАНИЕ:** WordPress по умолчанию ищет шаблоны страниц только в корне темы. Для поддержки шаблонов в подкаталоге `template-pages/` необходимо добавить специальные фильтры в `functions.php`.

**Задачи**:

1. **Добавить фильтр `theme_page_templates`** для отображения шаблонов в админке:
```php
/**
 * Добавляем шаблоны страниц из подкаталога template-pages/ в список доступных шаблонов
 */
add_filter('theme_page_templates', '{theme_slug}_add_page_templates_from_subdirectory');
function {theme_slug}_add_page_templates_from_subdirectory($templates) {
    $template_dir = get_template_directory() . '/template-pages/';
    
    if (is_dir($template_dir)) {
        $files = glob($template_dir . '*.php');
        
        foreach ($files as $file) {
            $filename = basename($file);
            $relative_path = 'template-pages/' . $filename;
            
            // Читаем заголовок шаблона из файла
            $file_data = get_file_data($file, array('Template Name' => 'Template Name'));
            
            if (!empty($file_data['Template Name'])) {
                $templates[$relative_path] = $file_data['Template Name'];
            }
        }
    }
    
    return $templates;
}
```

2. **Добавить фильтр `page_template_hierarchy`** для добавления в иерархию поиска:
```php
/**
 * Добавляем шаблоны из подкаталога в иерархию поиска шаблонов WordPress
 */
add_filter('page_template_hierarchy', '{theme_slug}_add_page_templates_to_hierarchy');
function {theme_slug}_add_page_templates_to_hierarchy($templates) {
    global $post;
    
    if (!$post || !isset($post->ID)) {
        return $templates;
    }
    
    $page_template = get_page_template_slug($post->ID);
    
    // Если выбран шаблон из подкаталога template-pages/, добавляем его в начало списка
    if (!empty($page_template) && strpos($page_template, 'template-pages/') === 0) {
        // Добавляем в начало списка, чтобы WordPress использовал его первым
        array_unshift($templates, $page_template);
    }
    
    return $templates;
}
```

3. **Добавить helper-функцию для поиска шаблонов и фильтры (ОБНОВЛЕНО)**:
```php
/**
 * Вспомогательная функция для поиска шаблона в подкаталоге
 * Проверяет несколько вариантов путей
 */
function {theme_slug}_locate_template_in_subdirectory($page_template) {
    if (empty($page_template)) {
        return null;
    }

    $normalized_template = ltrim($page_template, '/');
    $template_dir = {THEME_CONST_DIR}; // Замените на константу вашей темы

    $candidates = array(
        $template_dir . '/' . $normalized_template,
        $template_dir . '/template-pages/' . $normalized_template,
        $template_dir . '/template-pages/' . basename($normalized_template),
    );

    $checked = array();

    foreach ($candidates as $candidate) {
        $normalized_path = wp_normalize_path($candidate);

        if (isset($checked[$normalized_path])) {
            continue;
        }

        $checked[$normalized_path] = true;

        if (file_exists($candidate)) {
            return $candidate;
        }
    }

    return null;
}

/**
 * Обеспечиваем правильную загрузку шаблонов из подкаталога template-pages/
 */
add_filter('page_template', '{theme_slug}_load_template_from_subdirectory', 10);
function {theme_slug}_load_template_from_subdirectory($template) {
    if (!is_page()) {
        return $template;
    }

    global $post;

    if (!$post || !isset($post->ID)) {
        return $template;
    }

    $page_template = get_page_template_slug($post->ID);

    if (empty($page_template)) {
        return $template;
    }

    $resolved_template = {theme_slug}_locate_template_in_subdirectory($page_template);

    return $resolved_template ?: $template;
}
```

4. **Добавить фильтр `template_include` как fallback (ОБНОВЛЕНО)**:
```php
/**
 * Fallback фильтр на более позднем этапе
 * Использует ту же helper-функцию для поиска шаблона
 */
add_filter('template_include', '{theme_slug}_load_template_from_subdirectory_include', 20);
function {theme_slug}_load_template_from_subdirectory_include($template) {
    if (!is_page()) {
        return $template;
    }

    global $post;

    if (!$post || !isset($post->ID)) {
        return $template;
    }

    $template_basename = basename($template);
    if ($template_basename !== 'page.php' && $template_basename !== 'index.php') {
        return $template;
    }

    $page_template = get_page_template_slug($post->ID);

    if (empty($page_template)) {
        return $template;
    }

    $resolved_template = {theme_slug}_locate_template_in_subdirectory($page_template);

    return $resolved_template ?: $template;
}
```

**⚠️ ВАЖНО:**
- Все четыре фильтра должны быть добавлены в `functions.php`
- Фильтры должны быть зарегистрированы ДО использования шаблонов
- Используйте функцию `get_template_directory()` для определения пути к теме
- Поддерживаются как новые пути (`template-pages/page-home.php`), так и старые (`page-home.php`)
- Замените `{theme_slug}` на реальный slug вашей темы (например, `mytheme`, `business-theme`)

### 2.4 SEO и мета-теги

**Задачи**:

1. **Создать функцию для динамических мета-тегов**:
   - Open Graph
   - Twitter Cards
   - Schema.org JSON-LD

2. **Интеграция с популярными SEO плагинами**:
   - Yoast SEO
   - Rank Math
   - All in One SEO

---

## Фаза 3: Интеграция Carbon Fields

### 3.1 Инициализация Carbon Fields (inc/carbon-fields-config.php)

**Задачи**:

1. **Загрузка Carbon Fields**:
```php
use Carbon_Fields\Container;
use Carbon_Fields\Field;

add_action('after_setup_theme', 'crb_load');
function crb_load() {
    require_once(__DIR__ . '/../vendor/autoload.php');
    \Carbon_Fields\Carbon_Fields::boot();
}
```

2. **Регистрация всех кастомных полей**:
```php
add_action('carbon_fields_register_fields', '{theme_slug}_register_custom_fields');
```

### 3.2 Общие принципы создания полей Carbon Fields для переноса контента

**⚠️ О ПРЕДУПРЕЖДЕНИЯХ IDE (PHP0418):**
Carbon Fields использует динамический API через магические методы PHP (`__call`, `__get`), поэтому статический анализатор кода может показывать предупреждения о "несуществующих методах". **Эти предупреждения можно безопасно игнорировать**, так как методы существуют в рантайме. Распространенные предупреждения:
- `set_value_type()` на Image Field - метод наследуется от File_Field
- `set_rows()` на textarea/rich_text полях - метод доступен для этих типов полей
- `add_fields()` на Complex Field - метод доступен для Complex полей
- `set_options()` на Select Field - метод доступен для Select полей
- `set_context()`, `set_priority()`, `set_page_menu_position()` на Container - методы доступны для контейнеров

Если предупреждения мешают, можно добавить PHPDoc комментарии с аннотациями, но это необязательно.

**⚠️ КРИТИЧЕСКОЕ ТРЕБОВАНИЕ:** Весь контент статического сайта должен быть перенесен в динамическую тему через Carbon Fields. Это означает, что ВСЕ тексты, изображения, списки, таблицы, контакты и другие элементы, которые могут изменяться, должны стать редактируемыми через админку WordPress.

**Задачи**:

1. **Проанализировать структуру статического сайта**:
   - Определить все секции/блоки на каждой странице
   - Выявить повторяющиеся элементы (шапка, футер, контакты)
   - Определить список элементов (услуги, товары, отзывы, преимущества и т.д.)
   - Определить текстовые блоки, которые требуют форматирования (rich text)
   - Определить изображения, которые могут изменяться

2. **Создать контейнеры Carbon Fields для страниц**:
   ```php
   // Пример структуры контейнера для страницы
   function {theme_slug}_register_page_fields() {
       $container = Container::make('post_meta', __('Настройки страницы', '{theme_slug}'))
           ->where('post_type', '=', 'page');
       
       // ⚠️ ВАЖНО: Поддержка разных вариантов пути шаблона
       // WordPress может сохранить шаблон как 'template-pages/page-{slug}.php' или только 'page-{slug}.php'
       // Используем or_where() для поддержки обоих вариантов
       $container->where(
           function($condition) {
               $condition->where('post_template', '=', 'template-pages/page-{slug}.php')
                         ->or_where('post_template', '=', 'page-{slug}.php');
           }
       );
       
       // Добавляем поля для каждой секции статического сайта
       $container->add_fields(array(
           // Поля для секций добавляются здесь
       ));
   }
   ```

3. **Типы полей Carbon Fields для разных типов контента**:
   - **Текстовые заголовки и подзаголовки**: `Field::make('text', ...)` или `Field::make('textarea', ...)`
   - **Длинные тексты с форматированием**: `Field::make('rich_text', ...)` - используйте для текстов, которые содержат абзацы, списки, ссылки
   - **Изображения**: `Field::make('image', ...)->set_value_type('url')` или `->set_value_type('id')` в зависимости от требований
     - ⚠️ **Примечание**: Метод `set_value_type()` доступен для Image Field (наследуется от File_Field). Предупреждения IDE (PHP0418) о несуществующем методе можно игнорировать - метод существует в рантайме благодаря динамическому API Carbon Fields
   - **Select поля**: `Field::make('select', ...)->set_options(array(...))` - используйте `set_options()`, а НЕ `add_options()`
     - ⚠️ **ВАЖНО**: Метод называется `set_options()`, не `add_options()`
   - **Повторяющиеся списки элементов** (услуги, товары, отзывы и т.д.): `Field::make('complex', ...)`
   - **Контейнеры (Container)**: 
     - `Container::make('post_meta', ...)->set_context('normal')` - для post_meta контейнеров
     - `Container::make('post_meta', ...)->set_priority('high')` - установка приоритета
     - `Container::make('theme_options', ...)->set_page_menu_position(2)` - для theme_options
     - ⚠️ **Примечание**: Предупреждения IDE (PHP0418) о несуществующих методах Container можно игнорировать - методы существуют благодаря магическим методам PHP в Carbon Fields
   - **Телефоны и контакты**: `Field::make('text', ...)` - затем нормализуйте через helper-функцию
   - **Цены, числа, статистика**: `Field::make('text', ...)` или `Field::make('number', ...)`
   - **Иконки/классы CSS**: `Field::make('text', ...)` если используется Material Icons или другие наборы иконок

4. **Правила создания Complex полей для списков**:
   - Если в статическом HTML есть повторяющиеся блоки (например, карточки услуг), создайте `Complex` поле
   - Определите все данные внутри каждого блока (заголовок, описание, цена, иконка, изображение и т.д.)
   - Используйте `set_collapsed(false)` для начальных элементов (чтобы пользователь сразу видел данные)
   - Используйте `set_collapsed(true)` для новых элементов в списке
   - Установите `set_max()` если нужно ограничить количество элементов
   - **ВАЖНО:** Значения `set_default_value()` для Complex полей должны браться из статического HTML

5. **Правила установки значений по умолчанию**:
   - **ВСЕ** поля должны иметь `set_default_value()` с данными из статического сайта
   - Это позволяет пользователю сразу видеть контент при первом открытии админки
   - Для Complex полей передавайте массив массивов с данными из статического HTML
   - Используйте плейсхолдеры `{STATIC_*}` в документации для указания источника данных

6. **Структура полей для типовых секций статического сайта**:
   
   **Заголовок секции:**
   ```php
   Field::make('text', '{section}_title', __('Заголовок секции', '{theme_slug}'))
       ->set_default_value('{Заголовок из статического сайта}'),
   ```
   
   **Текстовый блок с форматированием:**
   ```php
   Field::make('rich_text', '{section}_text', __('Описание', '{theme_slug}'))
       ->set_default_value('{Текст из статического сайта}'),
   ```
   
   **Изображение секции:**
   ```php
   Field::make('image', '{section}_image', __('Изображение', '{theme_slug}'))
       ->set_value_type('url')
       ->set_default_value('{URL изображения из статического сайта}'),
   ```
   
   **Список элементов (Complex):**
   ```php
   Field::make('complex', '{section}_items', __('Элементы', '{theme_slug}'))
       ->add_fields(array(
           Field::make('text', 'item_title', __('Название', '{theme_slug}')),
           Field::make('textarea', 'item_description', __('Описание', '{theme_slug}')),
           // ... другие поля элемента
       ))
       ->set_default_value(array(
           // Массив данных из статического HTML
           array('item_title' => '{Название 1}', 'item_description' => '{Описание 1}'),
           array('item_title' => '{Название 2}', 'item_description' => '{Описание 2}'),
           // ...
       ))
       ->set_collapsed(false), // Для начальных элементов
   ```

7. **Организация полей в контейнерах**:
   - Группируйте поля по секциям статического сайта
   - Используйте `add_tab()` или `add_fields()` для логической группировки
   - Названия полей должны быть понятными и отражать их назначение
   - Используйте `set_help_text()` для сложных полей

8. **Обязательные требования к полям**:
   - Каждая секция статического сайта должна иметь соответствующие поля в Carbon Fields
   - Все изменяемые данные (тексты, изображения, списки) должны быть редактируемыми
   - Декоративные элементы, которые не изменяются, могут остаться в CSS/HTML
   - Контактная информация (телефоны, email, адреса) всегда должна быть редактируемой через Theme Options

### 3.3 Алгоритм создания полей для страницы

**Задачи**:

1. **Проанализировать HTML-структуру страницы статического сайта**:
   - Откройте файл страницы из `static_website/` (например, `index.html`, `about.html`)
   - Определите все `<section>` или другие структурные блоки
   - Для каждой секции определите:
     * Заголовок/подзаголовок
     * Текстовое содержимое
     * Изображения
     * Списки элементов (если есть)
     * Таблицы (если есть)
     * Формы (если есть)

2. **Создать контейнер Carbon Fields для страницы**:
   ```php
   function {theme_slug}_register_{page_slug}_fields() {
       $container = Container::make('post_meta', __('Настройки страницы {Page Name}', '{theme_slug}'))
           ->where('post_type', '=', 'page');
       
       // Поддержка разных вариантов пути шаблона
       $container->where(
           function($condition) {
               $condition->where('post_template', '=', 'template-pages/page-{slug}.php')
                         ->or_where('post_template', '=', 'page-{slug}.php');
           }
       );
       
       // Добавляем поля для каждой секции из статического HTML
       $container->add_fields(array(
           // Поля создаются на основе анализа статического HTML
       ));
   }
   ```

3. **Создать поля для каждой секции статического сайта**:
   - Для каждой секции из HTML создайте группу полей в Carbon Fields
   - Название полей должно отражать название секции (например, `{section_name}_title`, `{section_name}_text`)
   - Используйте префиксы для группировки полей одной секции

4. **Собрать данные из статического HTML**:
   - Скопируйте текстовые значения из HTML-атрибутов и содержимого тегов
   - Извлеките URL изображений из атрибутов `src`
   - Определите структуру списков элементов (из `<ul>`, `<ol>`, таблиц)
   - Зафиксируйте порядок секций на странице

5. **Установить значения по умолчанию**:
   - Все скопированные данные используйте в `set_default_value()`
   - Для Complex полей создайте массив с данными из статического HTML
   - Сохраните порядок элементов из оригинала

### 3.4 Создание Theme Options (глобальные настройки темы)

**⚠️ ВАЖНО:** Theme Options используются для данных, которые одинаковы на всех страницах сайта (шапка, футер, контакты, SEO, интеграции).

**Задачи**:

1. **Определить глобальные элементы статического сайта**:
   - Анализируйте `header.html`, `footer.html` или соответствующие секции в `index.html`
   - Определите контактную информацию (телефоны, email, адреса)
   - Определите тексты и описания, которые повторяются на разных страницах
   - Определите реквизиты компании, банковские данные, юридическую информацию
   - Определите настройки для SEO (Schema.org, Open Graph, Google Analytics)

2. **Создать контейнер Theme Options**:

   ```php
   Container::make('theme_options', __('Настройки темы {THEME_NAME}', '{theme_slug}'))
       ->set_page_menu_position(2)
       ->add_tab(__('Шапка сайта', '{theme_slug}'), array(
           // Поле для SVG логотипа (inline)
           Field::make('textarea', 'header_logo_svg', __('Логотип SVG (inline)', '{theme_slug}'))
               ->set_help_text('Вставьте SVG код логотипа напрямую. Например: <svg>...</svg>. Если поле не заполнено, будет использоваться иконка по умолчанию.')
               ->set_rows(10)
               ->set_width(100),
           
           // Поля для шапки (телефоны и т.д.)
           // Данные берутся из header.html или соответствующей секции
       ))
       ->add_tab(__('Подвал сайта', '{theme_slug}'), array(
           // Поле для SVG логотипа (inline)
           Field::make('textarea', 'footer_logo_svg', __('Логотип SVG (inline)', '{theme_slug}'))
               ->set_help_text('Вставьте SVG код логотипа напрямую. Например: <svg>...</svg>. Если поле не заполнено, будет использоваться иконка по умолчанию.')
               ->set_rows(10)
               ->set_width(100),
           
           // Поля для футера (описание, контакты, реквизиты и т.д.)
           // Данные берутся из footer.html или соответствующей секции
       ))
       ->add_tab(__('SEO и Schema.org', '{theme_slug}'), array(
           // Поля для SEO (название компании, рейтинг, Schema.org данные)
           // Данные определяются из анализа статического сайта
       ))
       ->add_tab(__('Интеграции', '{theme_slug}'), array(
           // Поля для интеграций (Telegram, Email, Google Analytics и т.д.)
       ))
       ->add_tab(__('Sitemap', '{theme_slug}'), array(
           // Поля для настройки XML Sitemap
           Field::make('checkbox', 'sitemap_enabled', __('Включить XML Sitemap', '{theme_slug}'))
               ->set_default_value(true)
               ->set_help_text('Включить или отключить генерацию XML Sitemap'),
           
           Field::make('separator', 'sitemap_exclude_separator', __('Исключения из Sitemap', '{theme_slug}')),
           
           Field::make('multiselect', 'sitemap_exclude_post_types', __('Исключить типы постов', '{theme_slug}'))
               ->set_options(function() {
                   $post_types = get_post_types(array('public' => true), 'objects');
                   $options = array();
                   foreach ($post_types as $post_type) {
                       $options[$post_type->name] = $post_type->label . ' (' . $post_type->name . ')';
                   }
                   return $options;
               })
               ->set_help_text('Выберите типы постов, которые нужно исключить из sitemap'),
           
           Field::make('multiselect', 'sitemap_exclude_taxonomies', __('Исключить таксономии', '{theme_slug}'))
               ->set_options(function() {
                   $taxonomies = get_taxonomies(array('public' => true), 'objects');
                   $options = array();
                   foreach ($taxonomies as $taxonomy) {
                       $options[$taxonomy->name] = $taxonomy->label . ' (' . $taxonomy->name . ')';
                   }
                   return $options;
               })
               ->set_help_text('Выберите таксономии, которые нужно исключить из sitemap'),
           
           Field::make('checkbox', 'sitemap_exclude_users', __('Исключить пользователей', '{theme_slug}'))
               ->set_default_value(false)
               ->set_help_text('Исключить страницы авторов из sitemap'),
           
           Field::make('text', 'sitemap_max_urls', __('Максимальное количество URL', '{theme_slug}'))
               ->set_default_value('2000')
               ->set_help_text('Максимальное количество URL в одном sitemap файле (по умолчанию 2000)')
               ->set_width(50),
       ));
   ```

3. **Принципы создания полей Theme Options**:
   - Все контактные данные из статического сайта (телефоны, email, адреса) должны быть редактируемыми
   - Используйте отдельные поля для видимого текста телефона и ссылки (для нормализации)
   - Реквизиты компании и банковские данные используйте `rich_text` поля
   - Все значения `set_default_value()` берутся из статического сайта
   - **Для SVG логотипов**: используйте `textarea` поля с `set_rows(10)` для удобного ввода SVG кода. При выводе используйте `wp_kses()` с массивом разрешенных тегов из функции `{theme_slug}_get_svg_allowed_tags()` для безопасного вывода SVG inline
   - **Для настройки Sitemap**: создайте вкладку "Sitemap" с полями для управления генерацией XML Sitemap. Реализуйте логику исключений в отдельном файле `inc/sitemap-settings.php`

5. **⚠️ ОБЯЗАТЕЛЬНО: Настройка XML Sitemap**:

   WordPress 5.5+ имеет встроенную генерацию XML Sitemap. Необходимо создать настройки для управления sitemap через админку.
   
   **Создание файла `inc/sitemap-settings.php`**:
   
   ```php
   <?php
   /**
    * Настройки Sitemap через Carbon Fields
    * 
    * @package {THEME_NAME}
    */
   
   if (!defined('ABSPATH')) {
       exit;
   }
   
   /**
    * Отключение sitemap если выключено в настройках
    */
   add_filter('wp_sitemaps_enabled', '{theme_slug}_sitemap_enabled');
   function {theme_slug}_sitemap_enabled($enabled) {
       $sitemap_enabled = {theme_slug}_theme_option('sitemap_enabled', true);
       return (bool) $sitemap_enabled;
   }
   
   /**
    * Исключение типов постов из sitemap
    */
   add_filter('wp_sitemaps_post_types', '{theme_slug}_exclude_post_types_from_sitemap');
   function {theme_slug}_exclude_post_types_from_sitemap($post_types) {
       $excluded_types = {theme_slug}_theme_option('sitemap_exclude_post_types', array());
       
       if (!empty($excluded_types) && is_array($excluded_types)) {
           foreach ($excluded_types as $post_type) {
               if (isset($post_types[$post_type])) {
                   unset($post_types[$post_type]);
               }
           }
       }
       
       return $post_types;
   }
   
   /**
    * Исключение таксономий из sitemap
    */
   add_filter('wp_sitemaps_taxonomies', '{theme_slug}_exclude_taxonomies_from_sitemap');
   function {theme_slug}_exclude_taxonomies_from_sitemap($taxonomies) {
       $excluded_taxonomies = {theme_slug}_theme_option('sitemap_exclude_taxonomies', array());
       
       if (!empty($excluded_taxonomies) && is_array($excluded_taxonomies)) {
           foreach ($excluded_taxonomies as $taxonomy) {
               if (isset($taxonomies[$taxonomy])) {
                   unset($taxonomies[$taxonomy]);
               }
           }
       }
       
       return $taxonomies;
   }
   
   /**
    * Исключение пользователей из sitemap
    */
   add_filter('wp_sitemaps_add_provider', '{theme_slug}_exclude_users_from_sitemap', 10, 2);
   function {theme_slug}_exclude_users_from_sitemap($provider, $name) {
       $exclude_users = {theme_slug}_theme_option('sitemap_exclude_users', false);
       
       if ($exclude_users && $name === 'users') {
           return null; // Предотвращает добавление провайдера пользователей
       }
       
       return $provider;
   }
   
   /**
    * Настройка максимального количества URL в sitemap
    */
   add_filter('wp_sitemaps_max_urls', '{theme_slug}_sitemap_max_urls', 10, 2);
   function {theme_slug}_sitemap_max_urls($max_urls, $object_type) {
       $custom_max = {theme_slug}_theme_option('sitemap_max_urls', '2000');
       $custom_max = intval($custom_max);
       
       if ($custom_max > 0) {
           return $custom_max;
       }
       
       return $max_urls;
   }
   ```
   
   **Подключение файла в `functions.php`**:
   
   ```php
   require_once CUSTOMGEORG_DIR . '/inc/sitemap-settings.php';
   ```
   
   **Почему это важно:**
   - Позволяет управлять sitemap через админку без редактирования кода
   - Дает возможность исключать ненужные типы постов, таксономии и пользователей
   - Улучшает SEO (контроль над индексацией)
   - Упрощает поддержку сайта

4. **🔧 Работа с SVG логотипами (inline)**:

   **Создание helper-функции для безопасного вывода SVG**:
   
   В файле `inc/helpers.php` добавьте функцию `{theme_slug}_get_svg_allowed_tags()`:
   
   ```php
   if (!function_exists('{theme_slug}_get_svg_allowed_tags')) {
       /**
        * Возвращает массив разрешенных тегов и атрибутов для безопасного вывода SVG.
        *
        * @return array
        */
       function {theme_slug}_get_svg_allowed_tags(): array
       {
           return array(
               'svg' => array(
                   'width' => true,
                   'height' => true,
                   'viewbox' => true,
                   'viewBox' => true,
                   'xmlns' => true,
                   'fill' => true,
                   'stroke' => true,
                   'class' => true,
                   'id' => true,
                   'style' => true,
               ),
               'path' => array(
                   'd' => true,
                   'fill' => true,
                   'stroke' => true,
                   'stroke-width' => true,
                   'stroke-linecap' => true,
                   'stroke-linejoin' => true,
                   'class' => true,
                   'id' => true,
               ),
               'circle' => array(
                   'cx' => true,
                   'cy' => true,
                   'r' => true,
                   'fill' => true,
                   'stroke' => true,
                   'stroke-width' => true,
                   'class' => true,
                   'id' => true,
               ),
               'rect' => array(
                   'x' => true,
                   'y' => true,
                   'width' => true,
                   'height' => true,
                   'fill' => true,
                   'stroke' => true,
                   'stroke-width' => true,
                   'rx' => true,
                   'ry' => true,
                   'class' => true,
                   'id' => true,
               ),
               'g' => array(
                   'fill' => true,
                   'stroke' => true,
                   'class' => true,
                   'id' => true,
                   'transform' => true,
               ),
               'polygon' => array(
                   'points' => true,
                   'fill' => true,
                   'stroke' => true,
                   'stroke-width' => true,
                   'class' => true,
                   'id' => true,
               ),
               'polyline' => array(
                   'points' => true,
                   'fill' => true,
                   'stroke' => true,
                   'stroke-width' => true,
                   'class' => true,
                   'id' => true,
               ),
               'line' => array(
                   'x1' => true,
                   'y1' => true,
                   'x2' => true,
                   'y2' => true,
                   'stroke' => true,
                   'stroke-width' => true,
                   'class' => true,
                   'id' => true,
               ),
               'ellipse' => array(
                   'cx' => true,
                   'cy' => true,
                   'rx' => true,
                   'ry' => true,
                   'fill' => true,
                   'stroke' => true,
                   'stroke-width' => true,
                   'class' => true,
                   'id' => true,
               ),
           );
       }
   }
   ```
   
   **Принципы работы с SVG логотипами**:
   
   - ⚠️ **Безопасность**: Всегда используйте `wp_kses()` для вывода SVG кода, чтобы предотвратить XSS атаки
   - ✅ **Fallback**: Если поле SVG не заполнено, используйте иконку по умолчанию (Material Icons или другое)
   - ✅ **Helper-функция**: Используйте `{theme_slug}_get_svg_allowed_tags()` для получения списка разрешенных SVG тегов и атрибутов
   - ✅ **Единообразие**: Используйте одну и ту же логику вывода SVG в header.php и footer.php
   
   **Пример использования в шаблонах**:
   
   ```php
   <?php
   // Получаем SVG логотип из настроек темы
   $logo_svg = {theme_slug}_theme_option('header_logo_svg', '');
   if (!empty($logo_svg)) {
       // Безопасный вывод SVG через wp_kses
       echo wp_kses($logo_svg, {theme_slug}_get_svg_allowed_tags());
   } else {
       // Fallback: иконка по умолчанию
       echo '<span class="material-icons" aria-hidden="true">local_shipping</span>';
   }
   ?>
   ```
   
   **Как использовать в админке**:
   
   1. Перейдите в **Внешний вид → Настройки темы {THEME_NAME}**
   2. Во вкладке **"Шапка сайта"** или **"Подвал сайта"** найдите поле **"Логотип SVG (inline)"**
   3. Вставьте SVG код напрямую, например:
      ```svg
      <svg width="100" height="100" viewBox="0 0 100 100" xmlns="http://www.w3.org/2000/svg">
        <circle cx="50" cy="50" r="40" fill="blue"/>
      </svg>
      ```
   4. Сохраните настройки
   
   **⚠️ Важно**: 
   - Если поле не заполнено, будет использоваться иконка по умолчанию
   - SVG код должен быть валидным XML
   - Все атрибуты и теги будут проверены через `wp_kses()` для безопасности

### 3.5 Работа со страницами, требующими особого подхода

**Задачи**:

1. **Для страниц с большим количеством текстового контента** (например, Privacy Policy, Terms of Service):
   - Анализируйте структуру страницы из `static_website/`
   - Определите все разделы/параграфы
   - Создайте отдельные `rich_text` поля для каждого раздела
   - Используйте табы или группы для организации полей
   - **ВАЖНО:** ВСЕ данные берутся из соответствующего HTML файла статического сайта

2. **Для страниц со сложной структурой**:
   - Разбейте страницу на логические секции
   - Создайте поля для каждой секции согласно алгоритму из раздела 3.3
   - Используйте `add_tab()` для группировки полей по секциям

3. **Для страниц с повторяющейся структурой** (каталог, блог):
   - Определите структуру элемента списка
   - Создайте `Complex` поле для списка элементов
   - Наполните `set_default_value()` данными из статического HTML

**⚠️ КРИТИЧЕСКОЕ ТРЕБОВАНИЕ:** 
- ВСЕ данные из статического сайта должны быть перенесены в Carbon Fields
- НЕТ страниц, которые редактируются только через стандартный редактор WordPress
- Даже страницы типа Privacy Policy должны редактироваться через Carbon Fields с данными из статического HTML

---

## 🎯 Продвинутая техника: Начальные данные для повторяемых полей (Complex Fields)

### Концепция

Когда вы создаёте **повторяемые поля** (Complex Fields) в Carbon Fields, вы можете добавлять начальные данные прямо в конфигурацию. Это значит, что когда создатель контента первый раз откроет админку, он уже найдёт готовые примеры элементов с полным содержимым вместо пустых полей.

### ⚠️ КРИТИЧЕСКОЕ ПРАВИЛО: Данные берутся из верстки статического сайта

**ВАЖНО:** При создании WordPress темы все значения (`set_default_value`) для повторяемых полей (Complex Fields) ВСЕГДА берутся из верстки статического сайта, который находится в директории `static_website/`.

**НЕ нужно:**
- ❌ Придумывать новые значения
- ❌ Использовать случайные примеры
- ❌ Писать свои тексты вместо оригинальных

**Нужно всегда:**
- ✅ Копировать текст, заголовки и описания из HTML верстки
- ✅ Использовать те же иконки и значения
- ✅ Сохранять структуру и порядок элементов
- ✅ Брать цены и цифры из исходных данных

**Пример правильного подхода:**
```php
// ✅ ПРАВИЛЬНО - данные из static_website/index.html
Field::make('complex', 'services_items', __('Услуги', '{theme_slug}'))
    ->set_default_value(array(
        array(
            'service_title' => '{Название услуги из HTML}',  // из HTML
            'service_icon' => 'local_shipping',                             // из HTML
            'service_description' => 'Транспортировка крупногабаритных...', // из HTML
            'service_price' => '{Цена услуги из HTML}',                       // из HTML
        ),
        // ... остальные элементы из HTML
    ))
```

### Требование: set_collapsed(false) для повторяемых полей

**ОБЯЗАТЕЛЬНО используйте `->set_collapsed(false)`** для всех Complex Fields, содержащих начальные данные!

**Почему это важно:**
- `set_collapsed(true)` скрывает элементы по умолчанию (все свёрнуто)
- `set_collapsed(false)` показывает элементы развёрнутыми (все развёрнуто)
- Когда пользователь видит готовые данные развёрнутыми, он понимает структуру
- Упрощает редактирование существующих элементов

**Правильно:**
```php
Field::make('complex', 'services_items', __('Услуги', '{theme_slug}'))
    ->set_default_value(array(...))
    ->set_collapsed(false)  // ✅ Элементы видны развёрнутыми
    ->set_max(10),
```

**Неправильно:**
```php
Field::make('complex', 'services_items', __('Услуги', '{theme_slug}'))
    ->set_default_value(array(...))
    ->set_collapsed(true)   // ❌ Элементы скрыты, пользователь их не видит!
    ->set_max(10),
```

### 🔧 Работа с rich_text полями

**ОБЯЗАТЕЛЬНОЕ ПРАВИЛО:** При использовании `Field::make('rich_text', ...)` в Carbon Fields вы работаете с полями, которые могут содержать HTML элементы, включая переносы строк `<br>`.

**В шаблонах PHP используйте ВСЕГДА эту комбинацию функций:**
```php
// ✅ ПРАВИЛЬНО
echo wp_kses_post(nl2br($rich_text_value));

// ❌ НЕПРАВИЛЬНО - потеряются переносы строк
echo wp_kses_post($rich_text_value);

// ❌ НЕПРАВИЛЬНО - потеряется HTML экранирование
echo nl2br($rich_text_value);
```

**Почему обе функции нужны:**
- `nl2br()` - преобразует переносы строк в `<br>` теги (обрабатывает перевод на новую строку)
- `wp_kses_post()` - позволяет безопасный HTML (включая `<br>`), но блокирует опасный код

### Как это работает

1. **Создание Complex Field**:
```php
Field::make('complex', 'field_name', 'Label')
    ->add_fields(array(
        Field::make('text', 'sub_field_1', 'Sub Field 1'),
        Field::make('text', 'sub_field_2', 'Sub Field 2'),
    ))
```

2. **Добавление начальных данных через `set_default_value()`**:
```php
->set_default_value(array(
    array(
        'sub_field_1' => 'Значение 1',
        'sub_field_2' => 'Значение 2',
    ),
    array(
        'sub_field_1' => 'Значение 3',
        'sub_field_2' => 'Значение 4',
    ),
))
```

### Практический пример: Services

```php
Field::make('complex', 'services_items', __('Услуги', '{theme_slug}'))
    ->add_fields(array(
        Field::make('text', 'service_title', __('Название услуги', '{theme_slug}'))
            ->set_width(60),
        Field::make('text', 'service_icon', __('Material Icon', '{theme_slug}'))
            ->set_default_value('local_shipping')
            ->set_width(40),
        Field::make('textarea', 'service_description', __('Описание услуги', '{theme_slug}'))
            ->set_rows(3),
        Field::make('text', 'service_price', __('Цена', '{theme_slug}'))
            ->set_help_text('Например: от 150 BYN за рейс')
            ->set_width(50),
    ))
    // ✅ ДОБАВЛЯЕМ НАЧАЛЬНЫЕ ДАННЫЕ
    ->set_default_value(array(
        array(
            'service_title' => '{Название услуги 1 из HTML}',
            'service_icon' => 'local_shipping',
            'service_description' => '{Описание услуги 1 из HTML}',
            'service_price' => '{Цена услуги 1 из HTML}',
        ),
        array(
            'service_title' => '{Название услуги 2 из HTML}',
            'service_icon' => 'delete_sweep',
            'service_description' => '{Описание услуги 2 из HTML}',
            'service_price' => '{Цена услуги 2 из HTML}',
        ),
    ))
    ->set_collapsed(true)
    ->set_max(10),
```

### Преимущества этого подхода

| Преимущество | Описание |
|---|---|
| **Без пустых полей** | Админ сразу видит примеры элементов, не нужно искать, что заполнять |
| **Быстрая настройка** | Создатель контента может сразу начать редактировать готовые примеры |
| **Документирование** | Сами элементы служат документацией о том, какой контент нужно заполнять |
| **Профессионально** | Сайт не выглядит пустым сразу после установки темы |
| **Демо-контент** | Готовые примеры показывают разработчику, как всё выглядит на фронтенде |

### Как данные хранятся в БД

Когда вы устанавливаете `set_default_value()` для Complex Field:

```
┌─────────────────────────────────────────────┐
│ WordPress Database (wp_postmeta)            │
├─────────────────────────────────────────────┤
│ post_id: 5                                  │
│ meta_key: services_items                    │
│ meta_value: (сериализованный массив)       │
│                                             │
│ Содержит:                                   │
│ [                                           │
│   {service_title: "{Услуга 1}..."},          │
│   {service_title: "{Услуга 2}"},        │
│   {service_title: "{Услуга 3}..."}          │
│ ]                                           │
└─────────────────────────────────────────────┘

         ↓ (при загрузке в админку)

┌─────────────────────────────────────────────┐
│ Carbon Fields Admin UI                      │
├─────────────────────────────────────────────┤
│ ✓ Элемент 1: {Услуга 1}...                 │
│ ✓ Элемент 2: {Услуга 2}                │
│ ✓ Элемент 3: {Услуга 3}...                 │
│ + Add Entry (для добавления новых)        │
└─────────────────────────────────────────────┘

         ↓ (при выводе на фронтенде)

┌─────────────────────────────────────────────┐
│ Фронтенд страница                           │
├─────────────────────────────────────────────┤
│ $items = carbon_get_post_meta(..., 'services_items')
│ // Вернёт массив с 3 услугами              │
│                                             │
│ foreach ($items as $item) {                │
│     echo $item['service_title'];           │
│ }                                           │
└─────────────────────────────────────────────┘
```

### Используемые Complex Fields в проекте

```
🔄 REPEATABLE FIELDS (требуют set_default_value)
├─ hero_features (особенности Hero)
├─ specifications_items (технические характеристики)
├─ about_stats (статистика)
├─ services_items (услуги) ✅ ДОБАВЛЕНЫ
├─ advantages_items (преимущества) ✅ ДОБАВЛЕНЫ
├─ how_it_works_steps (этапы работы)
└─ testimonials_items (отзывы) ✅ ДОБАВЛЕНЫ
```

### Получение данных в шаблоне

```php
<?php
// Получить все услуги с начальными значениями
$services = carbon_get_post_meta(get_the_ID(), 'services_items');

// Если ничего не было отредактировано - вернёт значения по умолчанию
foreach ($services as $service) {
    echo $service['service_title'];  // "{Название услуги из данных}"
    echo $service['service_price'];   // "{Цена услуги из данных}"
    // и т.д.
}
?>
```

### Важные моменты

⚠️ **Важно помнить**:
- Все ключи в массиве должны точно совпадать с именами подполей
- Массив `set_default_value()` должен быть массив массивов (для повторяемых полей)
- Данные сохраняются в БД как сериализованный PHP объект/массив
- При изменении значения в админке - оригинальный `set_default_value()` НЕ теряется
- Если пользователь удалит все элементы - при перезагрузке они вернутся (это можно изменить через код)

### Практический вывод в template-part

**Пример: template-parts/home/services-section.php**
```php
<?php
/**
 * Services Section Template Part
 * 
 * @package {THEME_NAME}
 */

// ОБНОВЛЕНО: Используем helper-функции (они сами проверяют доступность Carbon Fields)
$post_id = get_the_ID();
$services_title = {theme_slug}_post_meta($post_id, 'services_title', 'Наши услуги');
$services = {theme_slug}_post_meta($post_id, 'services_items', array());
?>

<section class="services">
    <h2><?php echo esc_html($services_title); ?></h2>
    
    <?php if (!empty($services)) : ?>
    <div class="services-grid">
        <?php foreach ($services as $service) : ?>
        <div class="service-card">
            <div class="service-icon">
                <span class="material-icons"><?php echo esc_html($service['service_icon']); ?></span>
            </div>
            <h3><?php echo esc_html($service['service_title']); ?></h3>
            <p><?php echo esc_html($service['service_description']); ?></p>
            <div class="service-price">
                <?php echo esc_html($service['service_price']); ?>
            </div>
        </div>
        <?php endforeach; ?>
    </div>
    <?php endif; ?>
</section>
```

**⚠️ ВАЖНО:**
- Используйте helper-функцию `{theme_slug}_post_meta()` вместо прямых вызовов `carbon_get_post_meta()`
- Helper-функции автоматически проверяют доступность Carbon Fields и возвращают fallback значения
- Всегда передавайте значения по умолчанию в helper-функции (из статического сайта)
- Используйте `get_the_ID()` один раз и сохраняйте в переменную для оптимизации
- При первом открытии админки пользователь увидит готовые данные и сможет их сразу редактировать!

---

## Фаза 4: Создание шаблонов WordPress

### 4.0 ⚠️ ОБЯЗАТЕЛЬНО: Создание шаблона 404 страницы

**Задачи**:

1. **Создать файл `404.php` в корне темы**:

   WordPress автоматически использует этот шаблон для всех страниц, которые не найдены (404 ошибка).
   
   ```php
   <?php
   /**
    * The template for displaying 404 pages (not found)
    *
    * @package {THEME_NAME}
    */
   
   get_header();
   ?>
   
   <main id="main" class="main main--page">
       <nav class="breadcrumbs" aria-label="Хлебные крошки" itemscope itemtype="https://schema.org/BreadcrumbList">
           <ol class="breadcrumbs__list">
               <li class="breadcrumbs__item" itemprop="itemListElement" itemscope itemtype="https://schema.org/ListItem">
                   <a href="<?php echo esc_url(home_url('/')); ?>" class="breadcrumbs__link" itemprop="item">
                       <span itemprop="name">Главная</span>
                   </a>
                   <meta itemprop="position" content="1" />
               </li>
               <li class="breadcrumbs__item breadcrumbs__item--current" aria-current="page" itemprop="itemListElement" itemscope itemtype="https://schema.org/ListItem">
                   <span class="breadcrumbs__current" itemprop="name">Страница не найдена</span>
                   <meta itemprop="position" content="2" />
               </li>
           </ol>
       </nav>
   
       <section class="about" id="error-404">
           <div class="about__container">
               <div class="about__content">
                   <div class="error-404">
                       <div class="error-404__icon">
                           <span class="material-icons" aria-hidden="true">error_outline</span>
                       </div>
                       <h1 class="about__title error-404__title">404</h1>
                       <h2 class="about__title about__title--spaced error-404__subtitle">Страница не найдена</h2>
                       <p class="about__text error-404__text">
                           К сожалению, запрашиваемая страница не существует или была перемещена. 
                           Возможно, вы ввели неправильный адрес или страница была удалена.
                       </p>
                       
                       <div class="error-404__actions">
                           <a href="<?php echo esc_url(home_url('/')); ?>" class="form-submit error-404__button">
                               <span class="material-icons" aria-hidden="true">home</span>
                               <span>Вернуться на главную</span>
                           </a>
                           <button type="button" class="form-submit error-404__button error-404__button--secondary" onclick="window.history.back();">
                               <span class="material-icons" aria-hidden="true">arrow_back</span>
                               <span>Назад</span>
                           </button>
                       </div>
   
                       <div class="error-404__search">
                           <h3 class="error-404__search-title">Попробуйте найти нужную информацию:</h3>
                           <?php get_search_form(); ?>
                       </div>
                   </div>
               </div>
           </div>
       </section>
   </main>
   
   <?php
   get_footer();
   ```
   
   **Принципы создания 404 страницы:**
   - Используйте структуру, аналогичную другим страницам темы (breadcrumbs, секции)
   - Добавьте понятное сообщение об ошибке
   - Предоставьте кнопки навигации (на главную, назад)
   - Добавьте форму поиска для помощи пользователю
   - Используйте существующие классы темы для единообразия стиля
   - Используйте Material Icons для визуальных элементов
   
   **Почему это важно:**
   - Улучшает пользовательский опыт (UX)
   - Предотвращает уход пользователей с сайта
   - Помогает поисковым системам правильно индексировать ошибки
   - Соответствует лучшим практикам WordPress

### 4.1 Создание header.php

**Задачи**:

1. **Интегрировать HTML структуру из index.html**:
   - Заменить статичный телефон на `carbon_get_theme_option('header_phone')`
   - Заменить навигацию на `wp_nav_menu()`
   - Добавить `wp_head()` перед `</head>`

2. **Пример кода**:
```php
<!DOCTYPE html>
<html <?php language_attributes(); ?>>
<head>
    <meta charset="<?php bloginfo('charset'); ?>">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <?php wp_head(); ?>
</head>
<body <?php body_class(); ?>>
<?php wp_body_open(); ?>

<header class="header">
    <div class="header__container">
        <a href="<?php echo esc_url(home_url('/')); ?>" class="header__logo" aria-label="На главную">
            <?php
            // Получаем SVG логотип из настроек темы
            $header_logo_svg = {theme_slug}_theme_option('header_logo_svg', '');
            if (!empty($header_logo_svg)) {
                // Безопасный вывод SVG через wp_kses с разрешенными тегами
                echo wp_kses($header_logo_svg, {theme_slug}_get_svg_allowed_tags());
            } else {
                // Иконка по умолчанию, если SVG не заполнен
                echo '<span class="material-icons" aria-hidden="true">local_shipping</span>';
            }
            ?>
        </a>
        
        <nav class="header__nav" id="headerNav">
            <?php
            wp_nav_menu(array(
                'theme_location' => 'primary',
                'container' => false,
                'menu_class' => 'header__menu',
                'items_wrap' => '<ul class="%2$s">%3$s</ul>',
            ));
            ?>
        </nav>
        
        <div class="header__contact">
            <div class="header__contacts">
                <?php
                // ОБНОВЛЕНО: Используем helper-функции
                $header_phone = {theme_slug}_theme_option('header_phone', '{STATIC_HEADER_PHONE}');
                $header_phone_link = {theme_slug}_theme_option('header_phone_link', '{STATIC_PHONE_DIGITS}');
                $header_tel_href = {theme_slug}_build_tel_href($header_phone_link, '{STATIC_PHONE_DIGITS}');
                
                if ($header_phone) :
                    ?>
                    <a href="<?php echo esc_url($header_tel_href); ?>" class="header__phone">
                        <?php echo esc_html($header_phone); ?>
                    </a>
                    <?php
                endif;
                ?>
            </div>
        </div>
        
        <!-- Theme Toggle & Mobile Menu -->
        <div class="header__tooglers">
            <div class="header__theme-toggle">
                <button class="theme-toggle" type="button">
                    <!-- SVG icons -->
                </button>
            </div>
            <button class="header__mobile-menu-btn" type="button">
                <span class="header__hamburger"></span>
            </button>
        </div>
    </div>
</header>
```

### 4.2 Создание footer.php

**Задачи**:

1. **Интегрировать HTML структуру footer**:
   - Заменить статичные данные на Carbon Fields
   - Добавить `wp_footer()` перед `</body>`

2. **КРИТИЧЕСКОЕ ПРАВИЛО: Обработка переносов строк в rich_text полях**

⚠️ **ВАЖНО:** Для полей типа `rich_text` в Carbon Fields используются переносы строк `<br>` или `\n`. При выводе этих полей в PHP шаблонах ОБЯЗАТЕЛЬНО используйте **`nl2br()`** функцию!

**Почему это нужно:**
- Carbon Fields сохраняет `<br>` теги в HTML редакторе
- При выводе на фронтенде эти теги должны быть экранированы
- `nl2br()` преобразует переносы строк в `<br>` теги
- `wp_kses_post()` позволяет `<br>` теги (безопасный вывод)

**Правильный способ вывода rich_text:**
```php
// ✅ ПРАВИЛЬНО - используем nl2br() + wp_kses_post()
<?php
$footer_legal = carbon_get_theme_option('footer_legal_info');
echo wp_kses_post(nl2br($footer_legal));
?>
```

**Неправильный способ:**
```php
// ❌ НЕПРАВИЛЬНО - без nl2br(), переносы теряются
<?php
$footer_legal = carbon_get_theme_option('footer_legal_info');
echo wp_kses_post($footer_legal);
?>
```

**Пример:**
```html
<!-- Статический сайт с явными <br> тегами -->
<p class="footer__legal-text">
    Карт-счет: {Номер счета из статического сайта} ({Валюта}), {Название банка}<br>
    БИК: {БИК банка из статического сайта}
</p>
```

**Как это выглядит в Carbon Fields (rich_text редактор):**
```
Карт-счет: BY81ALFA30132H00780010270000 (BYN), ЗАО «Альфа-Банк»
БИК: ALFABY2X
```

**Правильный вывод в footer.php:**
```php
<?php
$footer_bank = carbon_get_theme_option('footer_bank_info');
if ($footer_bank) : ?>
    <div class="footer__legal-info">
        <h4 class="footer__legal-title">Банковские реквизиты</h4>
        <p class="footer__legal-text">
            <?php echo wp_kses_post(nl2br($footer_bank)); ?>
        </p>
    </div>
<?php endif; ?>
```

**Результат в HTML:**
```html
<p class="footer__legal-text">
    Карт-счет: {Номер счета из статического сайта} ({Валюта}), {Название банка}<br>
    БИК: {БИК банка из статического сайта}
</p>
```

2. **Пример кода**:
```php
<footer class="footer">
    <div class="footer__container">
        <div class="footer__main">
            <div class="footer__brand-nav">
                <div class="footer__brand">
                    <a href="<?php echo esc_url(home_url('/')); ?>" class="footer__logo" aria-label="На главную">
                        <?php
                        // Получаем SVG логотип из настроек темы
                        $footer_logo_svg = {theme_slug}_theme_option('footer_logo_svg', '');
                        if (!empty($footer_logo_svg)) {
                            // Безопасный вывод SVG через wp_kses с разрешенными тегами
                            echo wp_kses($footer_logo_svg, {theme_slug}_get_svg_allowed_tags());
                        } else {
                            // Иконка по умолчанию, если SVG не заполнен
                            echo '<span class="material-icons" aria-hidden="true">local_shipping</span>';
                        }
                        ?>
                        <span class="footer__logo-text">{Название сайта}</span>
                    </a>
                    <p class="footer__description">
                        <?php echo esc_html({theme_slug}_theme_option('footer_description', '{STATIC_FOOTER_DESCRIPTION}')); ?>
                    </p>
                </div>
                <div class="footer__nav">
                    <?php
                    wp_nav_menu(array(
                        'theme_location' => 'footer',
                        'container' => false,
                        'menu_class' => 'footer__menu',
                    ));
                    ?>
                </div>
            </div>
            
            <div class="footer__contact">
                <h3 class="footer__contact-title">Контакты</h3>
                <!-- Динамический контент из Carbon Fields -->
            </div>
        </div>
        
        <div class="footer__bottom">
            <div class="footer__legal">
                <?php 
                // ОБНОВЛЕНО: Используем helper-функцию и правильный вывод rich_text
                $footer_legal = {theme_slug}_theme_option('footer_legal_info', '');
                if ($footer_legal) {
                    echo wp_kses_post(nl2br($footer_legal));
                }
                ?>
            </div>
            <p class="footer__copyright">
                <?php echo esc_html({theme_slug}_theme_option('footer_copyright', '{STATIC_COPYRIGHT}')); ?>
            </p>
        </div>
    </div>
</footer>

<!-- Модальные окна -->
<?php get_template_part('template-parts/modal', 'order'); ?>
<?php get_template_part('template-parts/floating', 'cta'); ?>

<?php wp_footer(); ?>
</body>
</html>
```

### 4.3 Создание template-parts/ для каждой секции

**⚠️ КРИТИЧЕСКОЕ ТРЕБОВАНИЕ:** Все файлы секций должны находиться в подкаталогах `template-parts/[page-type]/`, а НЕ напрямую в `template-parts/`!

**Структура:**
- `template-parts/home/` - секции главной страницы
- `template-parts/privacy/` - секции страницы Privacy (если нужны)
- `template-parts/shared/` - общие секции

**Задачи**:

1. **Создать `template-parts/home/hero-section.php`** (ОБНОВЛЕНО: используем helper-функции):
```php
<?php
$post_id = get_the_ID();
$hero_title = {theme_slug}_post_meta($post_id, 'hero_title', '{STATIC_HERO_TITLE}');
$hero_subtitle = {theme_slug}_post_meta($post_id, 'hero_subtitle', '{STATIC_HERO_SUBTITLE}');
$hero_bg_image = {theme_slug}_post_meta($post_id, 'hero_background_image', '{STATIC_HERO_BG_IMAGE}');
$hero_features = {theme_slug}_post_meta($post_id, 'hero_features', array());
?>

<section class="hero">
    <div class="hero__background">
        <img class="hero__background-img" 
             src="<?php echo esc_url($hero_bg_image); ?>" 
             alt="<?php echo esc_attr($hero_title); ?>" 
             loading="eager">
    </div>
    <div class="hero__container">
        <div class="hero__content">
            <h1 class="hero__title"><?php echo esc_html($hero_title); ?></h1>
            <p class="hero__subtitle"><?php echo esc_html($hero_subtitle); ?></p>
            
            <?php if (!empty($hero_features)) : ?>
            <div class="hero__features">
                <?php foreach ($hero_features as $feature) : ?>
                <div class="hero__feature">
                    <span class="material-icons"><?php echo esc_html($feature['feature_icon']); ?></span>
                    <span><?php echo esc_html($feature['feature_text']); ?></span>
                </div>
                <?php endforeach; ?>
            </div>
            <?php endif; ?>
            
            <!-- Кнопки CTA -->
        </div>
    </div>
</section>
```

2. **Аналогично создать шаблоны для всех секций главной страницы в `template-parts/home/`**:
   - `template-parts/home/specifications-section.php`
   - `template-parts/home/about-section.php`
   - `template-parts/home/services-section.php`
   - `template-parts/home/advantages-section.php`
   - `template-parts/home/how-it-works-section.php`
   - `template-parts/home/testimonials-section.php`
   - `template-parts/home/contacts-section.php`

**⚠️ ВАЖНО:**
- Все секции главной страницы находятся в `template-parts/home/`
- В шаблоне `template-pages/page-home.php` используйте: `get_template_part('template-parts/home/hero', 'section')`
- Общие секции (breadcrumbs, modals) размещайте в `template-parts/shared/`

### 4.4 Создание главного шаблона template-pages/page-home.php

**⚠️ КРИТИЧЕСКОЕ ТРЕБОВАНИЕ:** Шаблон ДОЛЖЕН находиться в каталоге `template-pages/`, а НЕ в корне темы!

**Задачи**:

1. **Создать шаблон главной страницы в каталоге `template-pages/`**:
   - Файл: `template-pages/page-home.php`
   - Все секции подключаются из `template-parts/home/`

2. **Пример кода**:
```php
<?php
/**
 * Template Name: Главная страница
 * Шаблон главной страницы с всеми секциями
 * 
 * @package {THEME_NAME}
 */

get_header();
?>

<main id="main" class="main">
    <?php
    // Подключаем секции главной страницы из template-parts/home/
    get_template_part('template-parts/home/hero', 'section');
    get_template_part('template-parts/home/specifications', 'section');
    get_template_part('template-parts/home/about', 'section');
    get_template_part('template-parts/home/services', 'section');
    get_template_part('template-parts/home/advantages', 'section');
    get_template_part('template-parts/home/how-it-works', 'section');
    get_template_part('template-parts/home/testimonials', 'section');
    get_template_part('template-parts/home/contacts', 'section');
    ?>
</main>

<?php
get_footer();
```

**⚠️ ВАЖНО:**
- Шаблон ОБЯЗАТЕЛЬНО должен быть в `template-pages/page-home.php`
- Все секции находятся в `template-parts/home/`
- Условие в Carbon Fields должно поддерживать разные варианты путей через `or_where()` (см. раздел 3.2)
- После создания шаблона обязательно перевыбрать его в настройках страницы в админке WordPress

### 4.5 Создание template-pages/page-privacy.php

**⚠️ КРИТИЧЕСКОЕ ТРЕБОВАНИЕ:** Страница Privacy Policy ТОЖЕ должна редактироваться через Carbon Fields, как и главная страница! Это не статичная страница!

**Задачи**:

1. **Создать шаблон в каталоге `template-pages/`** (НЕ в корне темы!):
   - Файл: `template-pages/page-privacy.php`
   - Использует Carbon Fields для редактирования контента
   - Все данные берутся из статического сайта `static_website/privacy.html`

2. **Пример кода**:
```php
<?php
/**
 * Template Name: Privacy Policy
 * Шаблон страницы политики конфиденциальности
 * 
 * @package {THEME_NAME}
 */

get_header();
?>

<main id="main" class="main main--page">
    <nav class="breadcrumbs" aria-label="Хлебные крошки" itemscope itemtype="https://schema.org/BreadcrumbList">
        <ol class="breadcrumbs__list">
            <li class="breadcrumbs__item" itemprop="itemListElement" itemscope itemtype="https://schema.org/ListItem">
                <a href="<?php echo esc_url(home_url('/')); ?>" class="breadcrumbs__link" itemprop="item">
                    <span itemprop="name">Главная</span>
                </a>
                <meta itemprop="position" content="1" />
            </li>
            <li class="breadcrumbs__item breadcrumbs__item--current" aria-current="page" itemprop="itemListElement" itemscope itemtype="https://schema.org/ListItem">
                <span class="breadcrumbs__current" itemprop="name">Политика конфиденциальности</span>
                <meta itemprop="position" content="2" />
            </li>
        </ol>
    </nav>
    
    <section class="about" id="policy">
        <div class="privacy__container">
            <div class="about__content">
                <h1 class="about__title">Политика конфиденциальности</h1>
                
                <?php
                // ОБНОВЛЕНО: Используем helper-функции
                $post_id = get_the_ID();
                $privacy_intro = {theme_slug}_post_meta($post_id, 'privacy_intro', '');
                $privacy_operator = {theme_slug}_post_meta($post_id, 'privacy_operator', '');
                
                if ($privacy_intro || $privacy_operator) :
                    ?>
                    <div class="entry-content">
                        <?php if ($privacy_intro) : ?>
                            <p class="about__text">
                                <?php echo wp_kses_post(nl2br($privacy_intro)); ?>
                            </p>
                        <?php endif; ?>
                        
                        <?php if ($privacy_operator) : ?>
                            <h2 class="about__title about__title--spaced">1. Оператор персональных данных</h2>
                            <p class="about__text">
                                <?php echo wp_kses_post(nl2br($privacy_operator)); ?>
                            </p>
                        <?php endif; ?>
                        
                        <!-- ... остальные разделы через Carbon Fields ... -->
                    </div>
                <?php else : ?>
                    <?php
                    // Fallback на стандартный контент WordPress
                    while (have_posts()) :
                        the_post();
                        ?>
                        <div class="entry-content">
                            <?php the_content(); ?>
                        </div>
                        <?php
                    endwhile;
                    ?>
                <?php endif; ?>
            </div>
        </div>
    </section>
</main>

<?php
get_footer();
```

**⚠️ ВАЖНО:**
- Шаблон ОБЯЗАТЕЛЬНО должен быть в `template-pages/page-privacy.php`
- ВСЕ данные (включая текст всех разделов) берутся из `static_website/privacy.html`
- Используется `rich_text` поля Carbon Fields для каждого раздела
- Обязательно использовать `wp_kses_post(nl2br(...))` для вывода rich_text полей

---

## Фаза 5: Обработка форм и AJAX

### 5.1 Создание вспомогательных функций санитизации (inc/form-helpers.php)

**Задачи**:

1. **Создать функции санитизации из статического сайта**:
```php
<?php
/**
 * Вспомогательные функции для обработки форм
 * Портированы из статического сайта (send-form.php)
 */

/**
 * Санитизация телефонного номера (ОБНОВЛЕНО: использует helper-функцию)
 * Делегирует в {theme_slug}_normalize_phone() для единообразия
 * 
 * @param string $phone Телефонный номер
 * @return string Очищенный телефон
 */
function {theme_slug}_sanitize_phone($phone) {
    if (!is_string($phone)) {
        return '';
    }
    
    // Используем helper-функцию нормализации
    return {theme_slug}_normalize_phone($phone);
}

/**
 * Санитизация текстовых полей
 * Удаляет лишние пробелы и экранирует HTML
 * 
 * @param string $text Текст для очистки
 * @return string Очищенный текст
 */
function {theme_slug}_sanitize_text($text) {
    return htmlspecialchars(trim($text), ENT_QUOTES, 'UTF-8');
}

/**
 * Валидация данных формы
 * 
 * @param string $name Имя
 * @param string $phone Телефон
 * @return array Массив с результатом валидации
 */
function {theme_slug}_validate_form_data($name, $phone) {
    $errors = array();
    
    // Проверка имени (минимум 2 символа)
    if (mb_strlen($name) < 2) {
        $errors[] = 'Имя должно содержать минимум 2 символа';
    }
    
    // Проверка телефона (минимум 7 цифр)
    if (mb_strlen($phone) < 7) {
        $errors[] = 'Введите корректный номер телефона';
    }
    
    return array(
        'valid' => empty($errors),
        'errors' => $errors,
    );
}
```

### 5.2 Создание обработчика Telegram (inc/telegram-handler.php)

**Задачи**:

1. **Создать функцию отправки в Telegram (портирована из send-form.php)**:
```php
<?php
/**
 * Обработчик отправки уведомлений в Telegram
 * Портирован из статического сайта (send-form.php)
 */

/**
 * Отправка данных в Telegram через Bot API
 * Использует file_get_contents с stream context для максимальной совместимости
 * 
 * @param string $name Имя отправителя
 * @param string $phone Телефон отправителя
 * @param string $message Сообщение (необязательно)
 * @return bool Успешность отправки
 */

/**
 * Формирует текст сообщения для Telegram Bot.
 * Вынесено в отдельную функцию для переиспользования
 */
function {theme_slug}_build_telegram_message($name, $phone, $message = '') {
    $message_parts = array(
        "🚛 <b>ЗАЯВКА НА МАНИПУЛЯТОР</b>",  // Замените на заголовок для вашего проекта
        "",
        '👤 Имя: ' . $name,
        '📞 Телефон: ' . $phone,
    );

    if (!empty($message)) {
        $message_parts[] = '📝 Сообщение: ' . $message;
    }

    $message_parts[] = '⏰ Время: ' . current_time('d.m.Y H:i:s');
    $message_parts[] = '🌐 Источник: сайт';

    return implode("\n", $message_parts);
}

function {theme_slug}_send_telegram_notification($name, $phone, $message = '') {
    // ОБНОВЛЕНО: Используем helper-функцию
    $bot_token = {theme_slug}_theme_option('telegram_bot_token');
    $chat_id = {theme_slug}_theme_option('telegram_chat_id');
    
    // Если настройки не заполнены, пропускаем отправку
    if (empty($bot_token) || empty($chat_id)) {
        error_log('{THEME_NAME}: Telegram credentials not configured');
        return false;
    }
    
    // Формируем сообщение через отдельную функцию
    $telegram_message = {theme_slug}_build_telegram_message($name, $phone, $message);
    
    // URL API Telegram
    $telegram_url = 'https://api.telegram.org/bot' . $bot_token . '/sendMessage';
    
    // Параметры для отправки
    $telegram_data = array(
        'chat_id' => $chat_id,
        'text' => $telegram_message,
        'parse_mode' => 'HTML',
    );
    
    // Настройка stream context (как в статическом сайте)
    $options = array(
        'http' => array(
            'header' => "Content-type: application/x-www-form-urlencoded\r\n",
            'method' => 'POST',
            'content' => http_build_query($telegram_data),
            'timeout' => 10,
        ),
    );
    
    $context = stream_context_create($options);
    
    // Отправка запроса
    $result = @file_get_contents($telegram_url, false, $context);
    
    // Проверка успешности отправки
    $success = false;
    if ($result !== false) {
        $response_data = @json_decode($result, true);
        // Telegram API возвращает {"ok": true} при успехе
        if (is_array($response_data) && isset($response_data['ok']) && $response_data['ok'] === true) {
            $success = true;
        } else {
            // Логируем ошибку если запрос выполнен, но ответ не успешный
            error_log('{THEME_NAME} Telegram: API вернул ошибку. Ответ: ' . $result);
        }
    } else {
        error_log('{THEME_NAME} Telegram: Не удалось выполнить запрос к API');
    }
    
    // Логирование для отладки
    if (defined('WP_DEBUG') && WP_DEBUG) {
        $log_data = array(
            'timestamp' => current_time('c'),
            'request' => $telegram_data,
            'response_headers' => isset($http_response_header) ? $http_response_header : array(),
            'response_body' => $result,
            'success' => $success,
        );
        
        error_log('{THEME_NAME} Telegram: ' . print_r($log_data, true));
        
        // Дополнительно логируем в файл (как в статическом сайте)
        $upload_dir = wp_upload_dir();
        $log_file = $upload_dir['basedir'] . '/{theme_slug}-tg-log.txt';
        $log_entry = date('c') . "\nЗапрос: " . print_r($telegram_data, true);
        $log_entry .= "\nОтвет: " . print_r($http_response_header ?? array(), true);
        $log_entry .= "\nТело: " . $result . "\n";
        $log_entry .= "Успех: " . ($success ? 'ДА' : 'НЕТ') . "\n\n";
        
        @file_put_contents($log_file, $log_entry, FILE_APPEND);
    }
    
    return $success;
}

/**
 * Альтернативная функция отправки через wp_remote_post
 * Можно использовать как fallback или для большей совместимости с WordPress
 * 
 * @param string $name Имя отправителя
 * @param string $phone Телефон отправителя
 * @param string $message Сообщение (необязательно)
 * @return bool Успешность отправки
 */
function {theme_slug}_send_telegram_notification_wp($name, $phone, $message = '') {
    // ОБНОВЛЕНО: Используем helper-функцию
    $bot_token = {theme_slug}_theme_option('telegram_bot_token');
    $chat_id = {theme_slug}_theme_option('telegram_chat_id');
    
    if (empty($bot_token) || empty($chat_id)) {
        return false;
    }
    
    // Формируем сообщение через отдельную функцию
    $telegram_message = {theme_slug}_build_telegram_message($name, $phone, $message);
    
    $url = "https://api.telegram.org/bot{$bot_token}/sendMessage";
    
    // Используем WordPress HTTP API
    $response = wp_remote_post($url, array(
        'timeout' => 10,
        'body' => array(
            'chat_id' => $chat_id,
            'text' => $telegram_message,
            'parse_mode' => 'HTML',
        ),
    ));
    
    if (is_wp_error($response)) {
        error_log('{THEME_NAME} Telegram Error: ' . $response->get_error_message());
        return false;
    }
    
    $response_code = wp_remote_retrieve_response_code($response);
    
    if (WP_DEBUG) {
        error_log('{THEME_NAME} Telegram Response Code: ' . $response_code);
        error_log('{THEME_NAME} Telegram Response Body: ' . wp_remote_retrieve_body($response));
    }
    
    return $response_code === 200;
}
```

### 5.3 Создание AJAX обработчика форм (inc/ajax-handlers.php)

**Задачи**:

1. **Создать AJAX handler с логикой из send-form.php**:
```php
<?php
/**
 * AJAX обработчики для форм
 * Портированы из статического сайта (send-form.php)
 */

// Подключаем вспомогательные функции
require_once get_template_directory() . '/inc/form-helpers.php';
require_once get_template_directory() . '/inc/telegram-handler.php';

// Регистрация AJAX действий
add_action('wp_ajax_{theme_slug}_submit_form', '{theme_slug}_handle_form_submission');
add_action('wp_ajax_nopriv_{theme_slug}_submit_form', '{theme_slug}_handle_form_submission');

/**
 * Обработчик отправки формы
 * Логика портирована из send-form.php
 */
function {theme_slug}_handle_form_submission() {
    // Проверка метода запроса (POST)
    if ($_SERVER['REQUEST_METHOD'] !== 'POST') {
        wp_send_json_error(array(
            'message' => 'Метод не поддерживается',
        ), 405);
        return;
    }
    
    // Проверка nonce для безопасности
    if (!check_ajax_referer('{theme_slug}-nonce', 'nonce', false)) {
        wp_send_json_error(array(
            'message' => 'Ошибка безопасности. Попробуйте обновить страницу.',
        ), 403);
        return;
    }
    
    // Проверка согласия на обработку данных (как в send-form.php)
    if (!isset($_POST['consent']) || $_POST['consent'] !== 'on') {
        wp_send_json_error(array(
            'message' => 'Необходимо согласие на обработку данных.',
        ), 400);
        return;
    }
    
    // Получение и санитизация данных
    $name = isset($_POST['name']) ? {theme_slug}_sanitize_text($_POST['name']) : '';
    $phone = isset($_POST['phone']) ? {theme_slug}_sanitize_phone($_POST['phone']) : '';
    $message = isset($_POST['message']) ? {theme_slug}_sanitize_text($_POST['message']) : '';
    
    // Валидация (как в send-form.php: минимум 2 символа для имени, 7 для телефона)
    $validation = {theme_slug}_validate_form_data($name, $phone);
    
    if (!$validation['valid']) {
        wp_send_json_error(array(
            'message' => 'Проверьте корректность введённых данных.',
        ), 400);
        return;
    }
    
    // Отправка в Telegram (основной канал уведомлений)
    $telegram_sent = {theme_slug}_send_telegram_notification($name, $phone, $message);
    
    // Отправка Email (дополнительно, если настроено)
    $email_sent = {theme_slug}_send_email_notification($name, $phone, $message);
    
    // Логирование результата
    if (WP_DEBUG) {
        error_log("{THEME_NAME} Form Submission - Telegram: " . ($telegram_sent ? 'OK' : 'FAIL') . 
                  ", Email: " . ($email_sent ? 'OK' : 'FAIL'));
    }
    
    // Ответ клиенту
    if ($telegram_sent) {
        wp_send_json_success(array(
            'message' => 'Спасибо! Ваша заявка принята. Мы свяжемся с вами в ближайшее время.',
        ));
    } else {
        wp_send_json_error(array(
            'message' => 'Ошибка отправки. Попробуйте позже.',
        ), 500);
    }
}

/**
 * Отправка Email уведомления (опционально)
 * 
 * @param string $name Имя
 * @param string $phone Телефон
 * @param string $message Сообщение
 * @return bool Успешность отправки
 */
function {theme_slug}_send_email_notification($name, $phone, $message) {
    // ОБНОВЛЕНО: Используем helper-функцию
    $recipient = {theme_slug}_theme_option('form_recipient_email');
    
    // Если email не настроен, пропускаем
    if (empty($recipient)) {
        return false;
    }
    
    $subject = 'Новая заявка с сайта - ' . get_bloginfo('name');
    
    // Формируем тело письма
    $body = "<html><body>";
    $body .= "<h2>Новая заявка с сайта</h2>";
    $body .= "<p><strong>Имя:</strong> {$name}</p>";
    $body .= "<p><strong>Телефон:</strong> {$phone}</p>";
    
    if (!empty($message)) {
        $body .= "<p><strong>Сообщение:</strong> {$message}</p>";
    }
    
    $body .= "<p><strong>Время:</strong> " . current_time('d.m.Y H:i:s') . "</p>";
    $body .= "<p><strong>IP:</strong> " . $_SERVER['REMOTE_ADDR'] . "</p>";
    $body .= "</body></html>";
    
    $headers = array(
        'Content-Type: text/html; charset=UTF-8',
        'From: ' . get_bloginfo('name') . ' <noreply@' . parse_url(home_url(), PHP_URL_HOST) . '>',
    );
    
    return wp_mail($recipient, $subject, $body, $headers);
}

/**
 * Установка правильных заголовков для AJAX ответов
 */
add_action('wp_ajax_{theme_slug}_submit_form', '{theme_slug}_set_ajax_headers', 1);
add_action('wp_ajax_nopriv_{theme_slug}_submit_form', '{theme_slug}_set_ajax_headers', 1);

function {theme_slug}_set_ajax_headers() {
    // Устанавливаем заголовки кэширования (как в send-form.php)
    header('Cache-Control: max-age=1800, public');
    header('Expires: ' . gmdate('D, d M Y H:i:s \G\M\T', time() + 1800));
}
```

### 5.4 Модификация JavaScript для работы с WordPress

**Задачи**:

1. **Обновить `assets/js/main.js`**:
   - Заменить `fetch('send-form.php')` на `fetch({theme_slug}Data.ajaxUrl)`
   - Добавить отправку `action` и `nonce` параметров
   - Обновить все пути к ресурсам
   - Сохранить всю логику валидации и UI из статического сайта
   - **ВАЖНО**: Убедиться, что функция `submitForm` определена в каждом модуле, который её использует (особенно для форм с ID, заканчивающимся на "Form")

2. **Пример модификации функции submitForm** (должна быть определена в каждом модуле, использующем её):
```javascript
/**
 * Отправляет форму на сервер через WordPress AJAX
 * Модифицированная версия из статического сайта
 * ВАЖНО: Эта функция должна быть определена в каждом модуле (IIFE), который её использует
 * @param {HTMLFormElement} form - Форма для отправки
 */
function submitForm(form) {
    // Проверяем наличие данных WordPress AJAX
    if (typeof {theme_slug}Data === 'undefined') {
        console.error('{THEME_NAME}: WordPress AJAX данные не загружены');
        showErrorMessage(form, 'Ошибка инициализации формы. Перезагрузите страницу.');
        return;
    }

    const formData = new FormData(form);
    
    // Добавляем WordPress специфичные параметры
    formData.append('action', '{theme_slug}_submit_form');
    formData.append('nonce', {theme_slug}Data.nonce);

    // Показ индикатора загрузки
    const submitBtn = form.querySelector('button[type="submit"]');
    const originalText = submitBtn ? submitBtn.innerHTML : '';
    if (submitBtn) {
        submitBtn.innerHTML = '<span class="material-icons" aria-hidden="true">hourglass_empty</span>Отправка...';
        submitBtn.disabled = true;
    }

    // Отправка на WordPress AJAX endpoint (вместо send-form.php)
    fetch({theme_slug}Data.ajaxUrl, {
        method: 'POST',
        body: formData
    })
    .then(response => response.json())
    .then(data => {
        if (data.success) {
            showSuccessMessage(form);
            form.reset();
        } else {
            // WordPress возвращает сообщение в data.data.message
            const errorMessage = data.data?.message || 'Произошла ошибка при отправке формы. Попробуйте еще раз.';
            showErrorMessage(form, errorMessage);
        }
    })
    .catch(error => {
        console.error('Ошибка отправки формы:', error);
        showErrorMessage(form, 'Произошла ошибка при отправке формы. Попробуйте позже.');
    })
    .finally(() => {
        if (submitBtn) {
            submitBtn.innerHTML = originalText;
            submitBtn.disabled = false;
        }
    });
}
```

3. **Обновить локализацию скриптов в functions.php**:
```php
// В inc/enqueue-scripts.php
function {theme_slug}_enqueue_scripts() {
    wp_enqueue_script(
        '{theme_slug}-main',
        get_template_directory_uri() . '/assets/js/main.js',
        array(),
        '1.0.0',
        true
    );
    
    // Локализация данных для JavaScript
    wp_localize_script('{theme_slug}-main', '{theme_slug}Data', array(
        'ajaxUrl' => admin_url('admin-ajax.php'),
        'nonce' => wp_create_nonce('{theme_slug}-nonce'),
        'siteUrl' => home_url('/'),
    ));
}
add_action('wp_enqueue_scripts', '{theme_slug}_enqueue_scripts');
```

### 5.5 Настройка Telegram credentials в админке

**Задачи**:

1. **Добавить инструкции в Theme Options**:
```php
// В inc/carbon-fields-config.php, в Theme Options
->add_tab(__('Формы и Email'), array(
    Field::make('html', 'telegram_instructions')
        ->set_html('
            <div style="background: #f0f6fc; border-left: 4px solid #0969da; padding: 15px; margin-bottom: 20px;">
                <h3 style="margin-top: 0;">📱 Настройка Telegram Bot</h3>
                <ol>
                    <li>Откройте <a href="https://t.me/BotFather" target="_blank">@BotFather</a> в Telegram</li>
                    <li>Отправьте команду <code>/newbot</code> и следуйте инструкциям</li>
                    <li>Скопируйте полученный токен и вставьте в поле ниже</li>
                    <li>Добавьте бота в ваш чат/канал и назначьте администратором</li>
                    <li>Используйте <a href="https://t.me/getmyid_bot" target="_blank">@getmyid_bot</a> для получения Chat ID</li>
                    <li>Вставьте Chat ID в поле ниже (с минусом для групп/каналов)</li>
                </ol>
            </div>
        '),
    
    Field::make('text', 'telegram_bot_token', 'Telegram Bot Token')
        ->set_help_text('Токен бота из @BotFather (формат: 1234567890:ABCdefGHIjklMNOpqrsTUVwxyz)')
        ->set_width(50),
    
    Field::make('text', 'telegram_chat_id', 'Telegram Chat ID')
        ->set_help_text('ID чата для получения уведомлений (формат: -1001234567890)')
        ->set_width(50),
    
    Field::make('html', 'telegram_test')
        ->set_html('
            <div style="margin-top: 10px;">
                <button type="button" id="test-telegram-btn" class="button button-secondary">
                    Отправить тестовое сообщение
                </button>
                <div id="telegram-test-result" style="margin-top: 10px;"></div>
            </div>
        '),
    
    Field::make('separator', 'email_separator', 'Email уведомления'),
    
    Field::make('text', 'form_recipient_email', 'Email получателя заявок')
        ->set_help_text('Email для дополнительных уведомлений (необязательно)')
        ->set_default_value(get_option('admin_email')),
))
```

2. **Создать AJAX endpoint для тестирования Telegram**:
```php
// В inc/ajax-handlers.php
add_action('wp_ajax_{theme_slug}_test_telegram', '{theme_slug}_test_telegram_connection');

function {theme_slug}_test_telegram_connection() {
    check_ajax_referer('{theme_slug}-admin-nonce', 'nonce');
    
    if (!current_user_can('manage_options')) {
        wp_send_json_error(array('message' => 'Недостаточно прав'));
    }
    
    $test_sent = {theme_slug}_send_telegram_notification(
        'Тест',
        '+1234567890',  // Замените на тестовый номер телефона
        'Это тестовое сообщение для проверки настройки Telegram Bot'
    );
    
    if ($test_sent) {
        wp_send_json_success(array(
            'message' => '✅ Тестовое сообщение успешно отправлено! Проверьте ваш Telegram чат.'
        ));
    } else {
        wp_send_json_error(array(
            'message' => '❌ Ошибка отправки. Проверьте Bot Token и Chat ID. Подробности в логах.'
        ));
    }
}
```

3. **Добавить JavaScript для кнопки тестирования**:
```javascript
// В отдельном файле assets/js/admin.js
jQuery(document).ready(function($) {
    $('#test-telegram-btn').on('click', function() {
        const button = $(this);
        const resultDiv = $('#telegram-test-result');
        
        button.prop('disabled', true).text('Отправка...');
        resultDiv.html('');
        
        $.ajax({
            url: ajaxurl,
            method: 'POST',
            data: {
                action: '{theme_slug}_test_telegram',
                nonce: {theme_slug}Admin.nonce
            },
            success: function(response) {
                if (response.success) {
                    resultDiv.html('<div class="notice notice-success inline"><p>' + response.data.message + '</p></div>');
                } else {
                    resultDiv.html('<div class="notice notice-error inline"><p>' + response.data.message + '</p></div>');
                }
            },
            error: function() {
                resultDiv.html('<div class="notice notice-error inline"><p>Ошибка соединения с сервером</p></div>');
            },
            complete: function() {
                button.prop('disabled', false).text('Отправить тестовое сообщение');
            }
        });
    });
});
```

4. **Подключить admin скрипты**:
```php
// В inc/enqueue-scripts.php
function {theme_slug}_enqueue_admin_scripts($hook) {
    // Загружаем только на странице настроек темы
    // Замените строку ниже на реальный hook страницы настроек темы
    if ($hook !== 'toplevel_page_crb_carbon_fields_container_nastrojki_temy_{theme_slug}') {
        return;
    }
    
    wp_enqueue_script(
        '{theme_slug}-admin',
        get_template_directory_uri() . '/assets/js/admin.js',
        array('jquery'),
        '1.0.0',
        true
    );
    
    wp_localize_script('{theme_slug}-admin', '{theme_slug}Admin', array(
        'nonce' => wp_create_nonce('{theme_slug}-admin-nonce'),
    ));
}
add_action('admin_enqueue_scripts', '{theme_slug}_enqueue_admin_scripts');
```

---

## Фаза 6: Каталог продуктов и блог (Custom Post Types)

**⚠️ ВАЖНО:** Данная фаза выполняется **ТОЛЬКО** если на статическом сайте обнаружен функционал каталога продуктов или блога с постами.

### 6.1 Определение наличия каталога продуктов или блога на статическом сайте

**Признаки каталога продуктов:**
- Наличие страниц/разделов с перечнем товаров/услуг/продуктов
- Карточки товаров с изображениями, названиями, описаниями, ценами
- Страницы отдельных товаров с детальной информацией
- Фильтры или категории товаров
- Поиск по товарам
- Пагинация списка товаров
- Ключевые слова в URL или навигации: `products`, `catalog`, `shop`, `items`, `товары`, `каталог`, `продукция`

**Признаки блога:**
- Раздел "Новости", "Блог", "Статьи", "Публикации"
- Список постов/статей с датами публикации, авторами, категориями
- Страницы отдельных постов/статей
- Категории и теги
- Архивы по датам (годы, месяцы)
- RSS feed
- Комментарии к постам

**⚠️ КРИТИЧЕСКОЕ ПРАВИЛО:** Анализируйте структуру статического сайта ДО начала трансформации:
1. Просмотрите все HTML файлы в `static_website/`
2. Изучите навигацию и меню сайта
3. Проверьте наличие повторяющихся паттернов (карточки, списки)
4. Определите, требуется ли создание Custom Post Types или можно использовать стандартные записи WordPress

### 6.2 Регистрация Custom Post Types для каталога продуктов

**Задачи:**

1. **Создать файл `inc/custom-post-types.php`**:

```php
<?php
/**
 * Custom Post Types
 * Регистрация кастомных типов постов для каталога продуктов и блога
 * 
 * @package {THEME_NAME}
 */

if (!defined('ABSPATH')) {
    exit;
}

/**
 * Регистрация Custom Post Type для продуктов
 * Используйте этот код ТОЛЬКО если на статическом сайте есть каталог продуктов
 */
function {theme_slug}_register_product_post_type() {
    // ⚠️ ВАЖНО: Замените 'product' на подходящий slug на основе анализа статического сайта
    // Примеры: 'product', 'service', 'item', 'catalog'
    
    $labels = array(
        'name'                  => __('Продукты', '{theme_slug}'),
        'singular_name'         => __('Продукт', '{theme_slug}'),
        'menu_name'             => __('Продукты', '{theme_slug}'),
        'name_admin_bar'        => __('Продукт', '{theme_slug}'),
        'add_new'               => __('Добавить новый', '{theme_slug}'),
        'add_new_item'          => __('Добавить новый продукт', '{theme_slug}'),
        'new_item'              => __('Новый продукт', '{theme_slug}'),
        'edit_item'             => __('Редактировать продукт', '{theme_slug}'),
        'view_item'             => __('Просмотреть продукт', '{theme_slug}'),
        'all_items'             => __('Все продукты', '{theme_slug}'),
        'search_items'          => __('Поиск продуктов', '{theme_slug}'),
        'not_found'             => __('Продукты не найдены', '{theme_slug}'),
        'not_found_in_trash'    => __('В корзине продуктов не найдено', '{theme_slug}'),
        'featured_image'        => __('Изображение продукта', '{theme_slug}'),
        'set_featured_image'    => __('Установить изображение продукта', '{theme_slug}'),
        'remove_featured_image' => __('Удалить изображение продукта', '{theme_slug}'),
    );

    $args = array(
        'labels'                => $labels,
        'public'                => true,
        'publicly_queryable'    => true,
        'show_ui'               => true,
        'show_in_menu'          => true,
        'query_var'             => true,
        'rewrite'               => array('slug' => 'products'), // ⚠️ Замените на slug из статического сайта
        'capability_type'       => 'post',
        'has_archive'           => true, // Включить архив (страница списка всех продуктов)
        'hierarchical'          => false,
        'menu_position'         => 20,
        'menu_icon'             => 'dashicons-products', // Или другой подходящий иконка
        'supports'              => array('title', 'editor', 'thumbnail', 'excerpt', 'custom-fields'),
        'show_in_rest'          => false, // Отключаем Gutenberg, используем Carbon Fields
    );

    register_post_type('product', $args); // ⚠️ Замените 'product' на нужный slug
}
add_action('init', '{theme_slug}_register_product_post_type');

/**
 * Регистрация таксономии для категорий продуктов
 */
function {theme_slug}_register_product_categories() {
    $labels = array(
        'name'              => __('Категории продуктов', '{theme_slug}'),
        'singular_name'     => __('Категория продукта', '{theme_slug}'),
        'search_items'      => __('Поиск категорий', '{theme_slug}'),
        'all_items'         => __('Все категории', '{theme_slug}'),
        'parent_item'        => __('Родительская категория', '{theme_slug}'),
        'parent_item_colon'  => __('Родительская категория:', '{theme_slug}'),
        'edit_item'         => __('Редактировать категорию', '{theme_slug}'),
        'update_item'       => __('Обновить категорию', '{theme_slug}'),
        'add_new_item'      => __('Добавить новую категорию', '{theme_slug}'),
        'new_item_name'     => __('Новое название категории', '{theme_slug}'),
        'menu_name'         => __('Категории', '{theme_slug}'),
    );

    $args = array(
        'hierarchical'      => true, // Иерархическая таксономия (как категории WordPress)
        'labels'            => $labels,
        'show_ui'           => true,
        'show_admin_column' => true,
        'query_var'         => true,
        'rewrite'           => array('slug' => 'product-category'), // ⚠️ Замените на нужный slug
    );

    register_taxonomy('product_category', array('product'), $args); // ⚠️ Замените 'product' на нужный slug
}
add_action('init', '{theme_slug}_register_product_categories');

/**
 * Регистрация таксономии для тегов продуктов (если требуется)
 */
function {theme_slug}_register_product_tags() {
    $labels = array(
        'name'              => __('Теги продуктов', '{theme_slug}'),
        'singular_name'     => __('Тег продукта', '{theme_slug}'),
        'search_items'      => __('Поиск тегов', '{theme_slug}'),
        'popular_items'     => __('Популярные теги', '{theme_slug}'),
        'all_items'         => __('Все теги', '{theme_slug}'),
        'edit_item'         => __('Редактировать тег', '{theme_slug}'),
        'update_item'       => __('Обновить тег', '{theme_slug}'),
        'add_new_item'      => __('Добавить новый тег', '{theme_slug}'),
        'new_item_name'     => __('Новое название тега', '{theme_slug}'),
        'menu_name'         => __('Теги', '{theme_slug}'),
    );

    $args = array(
        'hierarchical'      => false, // Неиерархическая таксономия (как теги WordPress)
        'labels'            => $labels,
        'show_ui'           => true,
        'show_admin_column' => true,
        'query_var'         => true,
        'rewrite'           => array('slug' => 'product-tag'),
    );

    register_taxonomy('product_tag', array('product'), $args);
}
// Раскомментируйте, если нужны теги:
// add_action('init', '{theme_slug}_register_product_tags');
```

2. **Подключить файл в `functions.php`**:

```php
// В functions.php, после подключения других inc файлов
require_once CUSTOMGEORG_DIR . '/inc/custom-post-types.php';
```

### 6.3 Регистрация Custom Post Type для блога (если требуется отдельный тип)

**⚠️ ВАЖНО:** WordPress уже имеет встроенный тип постов `post` для блога. Создавайте отдельный Custom Post Type ТОЛЬКО если:
- На статическом сайте есть ОТДЕЛЬНЫЙ раздел блога/новостей, который НЕ должен смешиваться со стандартными постами WordPress
- Требуется специфическая функциональность, отличная от стандартных постов

**Если требуется отдельный Custom Post Type для блога:**

```php
/**
 * Регистрация Custom Post Type для блога/новостей
 * Используйте ТОЛЬКО если требуется отдельный тип постов от стандартных WordPress постов
 */
function {theme_slug}_register_blog_post_type() {
    $labels = array(
        'name'                  => __('Статьи', '{theme_slug}'),
        'singular_name'         => __('Статья', '{theme_slug}'),
        'menu_name'             => __('Блог', '{theme_slug}'),
        // ... остальные labels аналогично продуктам
    );

    $args = array(
        'labels'                => $labels,
        'public'                => true,
        'publicly_queryable'    => true,
        'show_ui'               => true,
        'show_in_menu'          => true,
        'query_var'             => true,
        'rewrite'               => array('slug' => 'blog'), // ⚠️ Замените на нужный slug
        'capability_type'       => 'post',
        'has_archive'           => true,
        'hierarchical'          => false,
        'menu_position'         => 21,
        'menu_icon'             => 'dashicons-admin-post',
        'supports'              => array('title', 'editor', 'thumbnail', 'excerpt', 'comments', 'custom-fields'),
        'show_in_rest'          => false,
    );

    register_post_type('blog_post', $args); // ⚠️ Замените на нужный slug
}
// Раскомментируйте, если требуется отдельный тип для блога:
// add_action('init', '{theme_slug}_register_blog_post_type');
```

**Рекомендация:** Если блог простой, используйте стандартный тип постов WordPress (`post`) и стандартные категории/теги.

### 6.4 Интеграция Carbon Fields с Custom Post Types

**Задачи:**

1. **Создать мета-боксы Carbon Fields для продуктов**:

В файле `inc/carbon-fields-config.php`, после настройки Theme Options:

```php
/**
 * Мета-боксы для продуктов (Custom Post Type)
 * ⚠️ ВАЖНО: Используйте ТОЛЬКО если зарегистрирован Custom Post Type 'product'
 */
Container::make('post_meta', __('Дополнительная информация о продукте', '{theme_slug}'))
    ->where('post_type', '=', 'product') // ⚠️ Замените на ваш slug Custom Post Type
    ->add_fields(array(
        // Цена продукта (если требуется)
        Field::make('text', 'product_price', __('Цена', '{theme_slug}'))
            ->set_width(50)
            ->set_help_text(__('Укажите цену продукта (например: 1000 руб.)', '{theme_slug}')),
        
        // Скидка (если требуется)
        Field::make('text', 'product_discount', __('Скидка', '{theme_slug}'))
            ->set_width(50)
            ->set_help_text(__('Укажите скидку в процентах (например: 10%)', '{theme_slug}')),
        
        // Галерея изображений продукта
        Field::make('media_gallery', 'product_gallery', __('Галерея изображений', '{theme_slug}'))
            ->set_type('image')
            ->set_help_text(__('Добавьте дополнительные изображения продукта', '{theme_slug}')),
        
        // Характеристики продукта (повторяемое поле)
        Field::make('complex', 'product_specs', __('Характеристики продукта', '{theme_slug}'))
            ->set_collapsed(false)
            ->add_fields(array(
                Field::make('text', 'spec_name', __('Название характеристики', '{theme_slug}'))
                    ->set_width(50),
                Field::make('text', 'spec_value', __('Значение', '{theme_slug}'))
                    ->set_width(50),
            ))
            ->set_layout('tabbed-horizontal')
            ->set_header_template('<%- spec_name %>: <%- spec_value %>'),
        
        // Дополнительное описание (rich text)
        Field::make('rich_text', 'product_additional_info', __('Дополнительная информация', '{theme_slug}'))
            ->set_help_text(__('Подробное описание, технические характеристики и т.д.', '{theme_slug}')),
        
        // Статус наличия
        Field::make('select', 'product_availability', __('Наличие', '{theme_slug}'))
            ->set_options(array(
                'in_stock' => __('В наличии', '{theme_slug}'),
                'out_of_stock' => __('Нет в наличии', '{theme_slug}'),
                'pre_order' => __('Предзаказ', '{theme_slug}'),
            ))
            ->set_default_value('in_stock'),
    ));
```

2. **Пример мета-боксов для блога** (если используется Custom Post Type):

```php
/**
 * Мета-боксы для статей блога
 * ⚠️ ВАЖНО: Используйте ТОЛЬКО если зарегистрирован отдельный Custom Post Type для блога
 */
Container::make('post_meta', __('Дополнительная информация о статье', '{theme_slug}'))
    ->where('post_type', '=', 'blog_post') // ⚠️ Замените на ваш slug
    ->add_fields(array(
        Field::make('image', 'article_featured_image', __('Главное изображение статьи', '{theme_slug}'))
            ->set_help_text(__('Изображение, которое будет отображаться в списке статей и на странице статьи', '{theme_slug}')),
        
        Field::make('text', 'article_author', __('Автор статьи', '{theme_slug}'))
            ->set_width(50),
        
        Field::make('date', 'article_publish_date', __('Дата публикации', '{theme_slug}'))
            ->set_width(50)
            ->set_storage_format('Y-m-d'),
        
        Field::make('rich_text', 'article_excerpt', __('Краткое описание', '{theme_slug}'))
            ->set_help_text(__('Краткое описание статьи для списка статей', '{theme_slug}')),
    ));
```

### 6.5 Создание шаблонов для Custom Post Types

**Задачи:**

1. **Создать шаблон архива продуктов** (`archive-product.php`):

```php
<?php
/**
 * Template Name: Archive Products
 * Шаблон архива (списка) продуктов
 * 
 * @package {THEME_NAME}
 */

get_header();

// Получаем категорию, если выбрана фильтрация
$current_category = get_queried_object();
?>

<main class="main">
    <div class="container">
        <?php if ($current_category && is_tax()): ?>
            <h1 class="archive-title"><?php echo esc_html($current_category->name); ?></h1>
            <?php if ($current_category->description): ?>
                <div class="archive-description">
                    <?php echo wp_kses_post(nl2br($current_category->description)); ?>
                </div>
            <?php endif; ?>
        <?php else: ?>
            <h1 class="archive-title"><?php echo esc_html(get_the_archive_title()); ?></h1>
        <?php endif; ?>

        <div class="products-grid">
            <?php if (have_posts()): ?>
                <?php while (have_posts()): the_post(); ?>
                    <?php
                    // Используем helper-функции для получения мета-данных
                    $product_price = {theme_slug}_post_meta(get_the_ID(), 'product_price', '');
                    $product_image = get_the_post_thumbnail_url(get_the_ID(), 'large');
                    ?>
                    
                    <article class="product-card">
                        <?php if ($product_image): ?>
                            <div class="product-card__image">
                                <a href="<?php echo esc_url(get_permalink()); ?>">
                                    <img src="<?php echo esc_url($product_image); ?>" 
                                         alt="<?php echo esc_attr(get_the_title()); ?>">
                                </a>
                            </div>
                        <?php endif; ?>
                        
                        <div class="product-card__content">
                            <h2 class="product-card__title">
                                <a href="<?php echo esc_url(get_permalink()); ?>">
                                    <?php the_title(); ?>
                                </a>
                            </h2>
                            
                            <?php if (has_excerpt()): ?>
                                <div class="product-card__excerpt">
                                    <?php the_excerpt(); ?>
                                </div>
                            <?php endif; ?>
                            
                            <?php if ($product_price): ?>
                                <div class="product-card__price">
                                    <?php echo esc_html($product_price); ?>
                                </div>
                            <?php endif; ?>
                            
                            <a href="<?php echo esc_url(get_permalink()); ?>" 
                               class="product-card__link">
                                <?php _e('Подробнее', '{theme_slug}'); ?>
                            </a>
                        </div>
                    </article>
                <?php endwhile; ?>
                
                <?php
                // Пагинация
                the_posts_pagination(array(
                    'mid_size'  => 2,
                    'prev_text' => __('← Назад', '{theme_slug}'),
                    'next_text' => __('Вперед →', '{theme_slug}'),
                ));
                ?>
            <?php else: ?>
                <p><?php _e('Продукты не найдены', '{theme_slug}'); ?></p>
            <?php endif; ?>
        </div>
    </div>
</main>

<?php get_footer(); ?>
```

2. **Создать шаблон одиночного продукта** (`single-product.php`):

```php
<?php
/**
 * Template Name: Single Product
 * Шаблон страницы отдельного продукта
 * 
 * @package {THEME_NAME}
 */

get_header();

while (have_posts()): the_post();
    // Получаем мета-данные через helper-функции
    $product_price = {theme_slug}_post_meta(get_the_ID(), 'product_price', '');
    $product_discount = {theme_slug}_post_meta(get_the_ID(), 'product_discount', '');
    $product_gallery = {theme_slug}_post_meta(get_the_ID(), 'product_gallery', array());
    $product_specs = {theme_slug}_post_meta(get_the_ID(), 'product_specs', array());
    $product_additional_info = {theme_slug}_post_meta(get_the_ID(), 'product_additional_info', '');
    $product_availability = {theme_slug}_post_meta(get_the_ID(), 'product_availability', 'in_stock');
?>

<main class="main">
    <div class="container">
        <article class="product-single">
            <div class="product-single__header">
                <h1 class="product-single__title"><?php the_title(); ?></h1>
                
                <?php if ($product_price): ?>
                    <div class="product-single__price">
                        <?php echo esc_html($product_price); ?>
                        <?php if ($product_discount): ?>
                            <span class="product-single__discount">
                                <?php echo esc_html($product_discount); ?>
                            </span>
                        <?php endif; ?>
                    </div>
                <?php endif; ?>
                
                <div class="product-single__availability">
                    <?php
                    $availability_text = array(
                        'in_stock' => __('В наличии', '{theme_slug}'),
                        'out_of_stock' => __('Нет в наличии', '{theme_slug}'),
                        'pre_order' => __('Предзаказ', '{theme_slug}'),
                    );
                    echo esc_html($availability_text[$product_availability] ?? __('В наличии', '{theme_slug}'));
                    ?>
                </div>
            </div>
            
            <div class="product-single__gallery">
                <?php
                $main_image = get_the_post_thumbnail_url(get_the_ID(), 'full');
                if ($main_image):
                ?>
                    <div class="product-single__main-image">
                        <img src="<?php echo esc_url($main_image); ?>" 
                             alt="<?php echo esc_attr(get_the_title()); ?>">
                    </div>
                <?php endif; ?>
                
                <?php if (!empty($product_gallery)): ?>
                    <div class="product-single__gallery-thumbs">
                        <?php foreach ($product_gallery as $image_id): ?>
                            <?php
                            $image_url = wp_get_attachment_image_url($image_id, 'thumbnail');
                            $image_full = wp_get_attachment_image_url($image_id, 'full');
                            ?>
                            <a href="<?php echo esc_url($image_full); ?>" 
                               class="product-single__gallery-thumb">
                                <img src="<?php echo esc_url($image_url); ?>" 
                                     alt="<?php echo esc_attr(get_the_title()); ?>">
                            </a>
                        <?php endforeach; ?>
                    </div>
                <?php endif; ?>
            </div>
            
            <div class="product-single__content">
                <?php the_content(); ?>
            </div>
            
            <?php if (!empty($product_specs)): ?>
                <div class="product-single__specs">
                    <h2><?php _e('Характеристики', '{theme_slug}'); ?></h2>
                    <table class="specs-table">
                        <?php foreach ($product_specs as $spec): ?>
                            <tr>
                                <td><?php echo esc_html($spec['spec_name']); ?></td>
                                <td><?php echo esc_html($spec['spec_value']); ?></td>
                            </tr>
                        <?php endforeach; ?>
                    </table>
                </div>
            <?php endif; ?>
            
            <?php if ($product_additional_info): ?>
                <div class="product-single__additional">
                    <h2><?php _e('Дополнительная информация', '{theme_slug}'); ?></h2>
                    <div class="product-single__additional-content">
                        <?php echo wp_kses_post(nl2br($product_additional_info)); ?>
                    </div>
                </div>
            <?php endif; ?>
            
            <?php
            // Вывод категорий продукта
            $categories = get_the_terms(get_the_ID(), 'product_category');
            if ($categories && !is_wp_error($categories)):
            ?>
                <div class="product-single__categories">
                    <span><?php _e('Категории:', '{theme_slug}'); ?></span>
                    <?php
                    $category_links = array();
                    foreach ($categories as $category) {
                        $category_links[] = '<a href="' . esc_url(get_term_link($category)) . '">' . esc_html($category->name) . '</a>';
                    }
                    echo implode(', ', $category_links);
                    ?>
                </div>
            <?php endif; ?>
        </article>
    </div>
</main>

<?php
endwhile;
get_footer();
?>
```

3. **Создать шаблон категории продуктов** (`taxonomy-product_category.php`):

```php
<?php
/**
 * Template Name: Product Category Archive
 * Шаблон архива категории продуктов
 * 
 * @package {THEME_NAME}
 */

get_header();

$term = get_queried_object();
?>

<main class="main">
    <div class="container">
        <h1 class="archive-title"><?php echo esc_html($term->name); ?></h1>
        
        <?php if ($term->description): ?>
            <div class="archive-description">
                <?php echo wp_kses_post(nl2br($term->description)); ?>
            </div>
        <?php endif; ?>
        
        <?php
        // Используем тот же код, что и в archive-product.php
        // или включите через get_template_part()
        get_template_part('template-parts/archive', 'product');
        ?>
    </div>
</main>

<?php get_footer(); ?>
```

### 6.6 Использование стандартных постов WordPress для блога

**Если на статическом сайте есть блог, но НЕ требуется отдельный Custom Post Type:**

1. **Используйте стандартный тип постов `post`** WordPress
2. **Используйте стандартные категории** WordPress (через `register_taxonomy` можно кастомизировать)
3. **Создайте шаблоны:**
   - `archive.php` или `home.php` - для списка постов
   - `single.php` - для отдельного поста
   - `category.php` - для архива категории
   - `tag.php` - для архива тега

4. **Пример мета-боксов Carbon Fields для стандартных постов:**

```php
/**
 * Мета-боксы для стандартных постов WordPress (блог)
 */
Container::make('post_meta', __('Дополнительная информация', '{theme_slug}'))
    ->where('post_type', '=', 'post')
    ->add_fields(array(
        Field::make('image', 'post_featured_image', __('Дополнительное изображение', '{theme_slug}'))
            ->set_help_text(__('Изображение для социальных сетей (если отличается от миниатюры)', '{theme_slug}')),
        
        Field::make('text', 'post_author_custom', __('Автор', '{theme_slug}'))
            ->set_width(50),
        
        Field::make('checkbox', 'post_featured', __('Популярная статья', '{theme_slug}')),
    ));
```

### 6.7 Алгоритм определения и реализации

**Шаг 1: Анализ статического сайта**
```
1. Просмотреть все HTML файлы в static_website/
2. Найти повторяющиеся паттерны:
   - Карточки товаров/продуктов
   - Списки статей/новостей
   - Страницы отдельных товаров/статей
3. Определить URL структуру:
   - /products/ или /catalog/ → каталог продуктов
   - /blog/ или /news/ → блог
   - /products/item-name.html → отдельный продукт
   - /blog/post-name.html → отдельная статья
4. Проверить наличие фильтров/категорий
```

**Шаг 2: Решение о реализации**
```
ЕСЛИ найден каталог продуктов:
  → Создать Custom Post Type 'product' (или подходящий slug)
  → Создать таксономию 'product_category'
  → Создать мета-боксы Carbon Fields для продуктов
  → Создать шаблоны archive-product.php и single-product.php

ЕСЛИ найден блог:
  → Решить: Custom Post Type или стандартные посты WordPress?
  → ЕСЛИ требует отдельной функциональности:
    → Создать Custom Post Type для блога
  → ИНАЧЕ:
    → Использовать стандартные посты WordPress (post)
  → Создать соответствующие шаблоны
```

**Шаг 3: Перенос контента**
```
1. Для каждого продукта/поста на статическом сайте:
   - Создать новую запись в WordPress
   - Заполнить заголовок, описание (из HTML)
   - Добавить изображения
   - Заполнить мета-поля Carbon Fields
   - Назначить категории/теги

2. Сохранить структуру URL через rewrite rules (если требуется)
```

### 6.8 Важные замечания

**⚠️ КРИТИЧЕСКИЕ ПРАВИЛА:**

1. **Всегда используйте helper-функции** для получения мета-данных:
   ```php
   // ПРАВИЛЬНО:
   $price = {theme_slug}_post_meta($post_id, 'product_price', '');
   
   // НЕПРАВИЛЬНО:
   $price = carbon_get_post_meta($post_id, 'product_price');
   ```

2. **Проверяйте существование значений:**
   ```php
   $gallery = {theme_slug}_post_meta(get_the_ID(), 'product_gallery', array());
   if (!empty($gallery)) {
       // Обработка галереи
   }
   ```

3. **Используйте правильные функции для вывода:**
   ```php
   // Rich text поля:
   echo wp_kses_post(nl2br($rich_text_field));
   
   // Обычные текстовые поля:
   echo esc_html($text_field);
   
   // Массивы (Complex Fields):
   foreach ($specs as $spec) {
       echo esc_html($spec['spec_name']);
   }
   ```

4. **Настройте permalink структуру** в WordPress: Настройки → Постоянные ссылки

5. **Используйте `has_archive => true`** только если требуется страница архива

6. **Правильно настройте rewrite rules:**
   ```php
   'rewrite' => array(
       'slug' => 'products', // URL slug
       'with_front' => false, // Не добавлять префикс блога
       'feeds' => true, // Включить RSS
       'pages' => true, // Поддержка пагинации
   ),
   ```

---

## Фаза 7: SEO и Schema.org интеграция

### 7.1 Создание функций для Schema.org разметки

**Задачи**:

1. **Создать функцию для JSON-LD**:
```php
function {theme_slug}_output_schema_org() {
    if (!is_front_page()) {
        return;
    }
    
    // ОБНОВЛЕНО: Используем helper-функции
    $schema = array(
        '@context' => 'https://schema.org',
        '@type' => 'LocalBusiness',
        'name' => {theme_slug}_theme_option('business_name', '{STATIC_COMPANY_NAME}'),
        'legalName' => {theme_slug}_theme_option('business_legal_name', '{STATIC_LEGAL_NAME}'),
        'taxID' => {theme_slug}_theme_option('business_tax_id', '{STATIC_TAX_ID}'),
        'url' => home_url('/'),
        'telephone' => {theme_slug}_theme_option('header_phone_link', '{STATIC_PHONE_DIGITS}'),
        'email' => {theme_slug}_theme_option('footer_email', '{STATIC_EMAIL}'),
        'address' => array(
            '@type' => 'PostalAddress',
            'streetAddress' => '{Улица и дом из настроек}',
            'addressLocality' => '{Город из настроек}',
            'addressRegion' => '{Область/штат из настроек}',
            'postalCode' => '{Почтовый индекс из настроек}',
            'addressCountry' => '{Код страны из настроек (например: US, BY, RU)}',
        ),
        'geo' => array(
            '@type' => 'GeoCoordinates',
            'latitude' => {theme_slug}_post_meta(get_the_ID(), 'map_latitude', '{STATIC_LATITUDE}'),
            'longitude' => {theme_slug}_post_meta(get_the_ID(), 'map_longitude', '{STATIC_LONGITUDE}'),
        ),
        'openingHours' => 'Mo-Su 00:00-23:59',
        'priceRange' => '$$',
        'aggregateRating' => array(
            '@type' => 'AggregateRating',
            'ratingValue' => {theme_slug}_theme_option('business_rating', '5.0'),
            'reviewCount' => {theme_slug}_theme_option('business_review_count', '100'),
        ),
    );
    
    // ОБНОВЛЕНО: Используем helper-функцию для услуг
    $services = {theme_slug}_post_meta(get_the_ID(), 'services_items', array());
    if (!empty($services)) {
        $schema['hasOfferCatalog'] = array(
            '@type' => 'OfferCatalog',
            'name' => '{Название каталога услуг}',
            'itemListElement' => array(),
        );
        
        foreach ($services as $service) {
            $schema['hasOfferCatalog']['itemListElement'][] = array(
                '@type' => 'Offer',
                'itemOffered' => array(
                    '@type' => 'Service',
                    'name' => $service['service_title'],
                    'description' => $service['service_description'],
                ),
            );
        }
    }
    
    echo '<script type="application/ld+json">' . wp_json_encode($schema, JSON_UNESCAPED_UNICODE) . '</script>';
}
add_action('wp_head', '{theme_slug}_output_schema_org');
```

### 7.2 Создание функций для Open Graph и Twitter Cards

**Задачи**:

1. **Создать функцию для OG тегов**:
```php
function {theme_slug}_output_og_tags() {
    // ОБНОВЛЕНО: Используем helper-функцию
    $og_image = {theme_slug}_theme_option('og_default_image', {theme_slug}_asset_uri('images/default-og.webp'));
    $og_title = is_front_page() ? get_bloginfo('name') : get_the_title();
    $og_description = is_front_page() ? get_bloginfo('description') : get_the_excerpt();
    ?>
    <meta property="og:title" content="<?php echo esc_attr($og_title); ?>">
    <meta property="og:description" content="<?php echo esc_attr($og_description); ?>">
    <meta property="og:type" content="website">
    <meta property="og:url" content="<?php echo esc_url(get_permalink()); ?>">
    <meta property="og:image" content="<?php echo esc_url($og_image); ?>">
    <meta property="og:site_name" content="<?php echo esc_attr(get_bloginfo('name')); ?>">
    <meta property="og:locale" content="ru_BY">
    
    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:title" content="<?php echo esc_attr($og_title); ?>">
    <meta name="twitter:description" content="<?php echo esc_attr($og_description); ?>">
    <meta name="twitter:image" content="<?php echo esc_url($og_image); ?>">
    <?php
}
add_action('wp_head', '{theme_slug}_output_og_tags', 5);
```

**ОБНОВЛЕНО:** Теперь OG и Twitter теги используют кастомные функции для получения title и description из Carbon Fields (см. раздел 7.3).

### 7.3 Настройка Meta Title и Description для каждой страницы ✅ **ДОБАВЛЕНО**

**Задачи:**

1. **Добавить поля Carbon Fields для SEO на всех страницах**:

В файле `inc/carbon-fields-config.php`:

```php
/**
 * Поля SEO для всех страниц (meta title и description)
 */
function {theme_slug}_register_seo_fields() {
    // Контейнер для всех страниц
    $container = Container::make('post_meta', __('SEO настройки', '{theme_slug}'))
        ->where('post_type', '=', 'page')
        ->set_context('normal')
        ->set_priority('high');
    
    $container->add_fields(array(
        Field::make('text', 'meta_title', __('Meta Title', '{theme_slug}'))
            ->set_help_text(__('Заголовок страницы для поисковых систем (рекомендуется 50-60 символов). Если не заполнено, будет использоваться заголовок страницы.', '{theme_slug}'))
            ->set_width(100),
        
        Field::make('textarea', 'meta_description', __('Meta Description', '{theme_slug}'))
            ->set_help_text(__('Описание страницы для поисковых систем (рекомендуется 150-160 символов). Если не заполнено, будет использоваться отрывок или часть контента.', '{theme_slug}'))
            ->set_rows(3)
            ->set_width(100),
    ));
}
```

**ВАЖНО:** Добавьте вызов этой функции в `{theme_slug}_register_custom_fields()`:

```php
function {theme_slug}_register_custom_fields() {
    // ... другие функции регистрации ...
    {theme_slug}_register_seo_fields();
}
```

2. **Создать функции для получения meta title и description**:

В файле `inc/seo-functions.php`:

```php
/**
 * Отключение стандартного вывода title WordPress
 */
add_filter('pre_get_document_title', '{theme_slug}_replace_document_title', 10);
function {theme_slug}_replace_document_title($title) {
    // Возвращаем пустую строку, чтобы WordPress не выводил title через wp_head
    // Наш кастомный title будет выведен через {theme_slug}_output_custom_title()
    return '';
}

/**
 * Получение meta title для текущей страницы
 * 
 * @return string
 */
function {theme_slug}_get_meta_title() {
    if (is_front_page() || is_home()) {
        $post_id = get_the_ID();
        if ($post_id) {
            $meta_title = {theme_slug}_post_meta($post_id, 'meta_title', '');
            if (!empty($meta_title)) {
                return $meta_title;
            }
        }
        return get_bloginfo('name');
    }
    
    if (is_singular()) {
        $meta_title = {theme_slug}_post_meta(get_the_ID(), 'meta_title', '');
        if (!empty($meta_title)) {
            return $meta_title;
        }
        return get_the_title();
    }
    
    if (is_category() || is_tag() || is_tax()) {
        return get_the_archive_title();
    }
    
    if (is_archive()) {
        return get_the_archive_title();
    }
    
    if (is_search()) {
        return sprintf(__('Результаты поиска: %s', '{theme_slug}'), get_search_query());
    }
    
    if (is_404()) {
        return __('Страница не найдена', '{theme_slug}');
    }
    
    return get_bloginfo('name');
}

/**
 * Получение meta description для текущей страницы
 * 
 * @return string
 */
function {theme_slug}_get_meta_description() {
    // Дефолтное описание для главной страницы
    // ⚠️ ВАЖНО: Замените на описание из статического сайта
    $default_home_description = '{STATIC_META_DESCRIPTION}'; // Например: 'Профессиональная аренда манипулятора в Могилеве. Звоните +375-33-662-24-41. Быстрая подача, надежная работа.'
    
    if (is_front_page() || is_home()) {
        $post_id = get_the_ID();
        if ($post_id) {
            $meta_description = {theme_slug}_post_meta($post_id, 'meta_description', '');
            if (!empty($meta_description)) {
                return $meta_description;
            }
        }
        return $default_home_description;
    }
    
    if (is_singular()) {
        $meta_description = {theme_slug}_post_meta(get_the_ID(), 'meta_description', '');
        if (!empty($meta_description)) {
            return $meta_description;
        }
        
        // Пытаемся получить отрывок
        $excerpt = get_the_excerpt();
        if (!empty($excerpt)) {
            return wp_trim_words($excerpt, 25, '...');
        }
        
        // Пытаемся получить часть контента
        $content = get_the_content();
        $content = wp_strip_all_tags($content);
        if (!empty($content)) {
            return wp_trim_words($content, 25, '...');
        }
    }
    
    if (is_category() || is_tag() || is_tax()) {
        $term_description = get_the_archive_description();
        if (!empty($term_description)) {
            return wp_trim_words(wp_strip_all_tags($term_description), 25, '...');
        }
    }
    
    if (is_search()) {
        return sprintf(__('Результаты поиска по запросу: %s', '{theme_slug}'), get_search_query());
    }
    
    return $default_home_description;
}

/**
 * Вывод кастомного title в head
 */
function {theme_slug}_output_custom_title() {
    $title = {theme_slug}_get_meta_title();
    echo '<title>' . esc_html($title) . '</title>' . "\n";
}
add_action('wp_head', '{theme_slug}_output_custom_title', 1);

/**
 * Вывод meta description в head
 */
function {theme_slug}_output_meta_description() {
    $description = {theme_slug}_get_meta_description();
    echo '<meta name="description" content="' . esc_attr($description) . '">' . "\n";
}
add_action('wp_head', '{theme_slug}_output_meta_description', 2);
```

3. **Обновить функцию вывода OG тегов**:

В функции `{theme_slug}_output_og_tags()` заменить:

```php
// СТАРЫЙ КОД:
$og_title = is_front_page() ? get_bloginfo('name') : get_the_title();
$og_description = is_front_page() ? get_bloginfo('description') : get_the_excerpt();

// НОВЫЙ КОД:
// Используем кастомные функции для получения title и description
$og_title = {theme_slug}_get_meta_title();
$og_description = {theme_slug}_get_meta_description();

// Для страниц пытаемся получить изображение из поста
if (is_singular() && has_post_thumbnail()) {
    $thumbnail_url = get_the_post_thumbnail_url(get_the_ID(), 'large');
    if ($thumbnail_url) {
        $og_image = $thumbnail_url;
    }
}
```

**⚠️ ВАЖНО:**
- Дефолтное значение для meta description главной страницы берется из статического сайта
- Всегда используйте helper-функции `{theme_slug}_post_meta()` для получения значений из Carbon Fields
- Если поля не заполнены, используются fallback значения (заголовок страницы для title, отрывок/контент для description)
- Для главной страницы, если meta_description не заполнено, используется дефолтное значение из функции

---

## Фаза 8: Оптимизация производительности

### 8.1 Оптимизация загрузки ресурсов

**Задачи**:

1. **Добавить preload для критических ресурсов**:
```php
function {theme_slug}_add_resource_hints() {
    // ОБНОВЛЕНО: Используем helper-функции для URI
    $preload_fonts = array(
        'fonts/inter/Inter-Regular.woff2',
        'fonts/inter/Inter-Medium.woff2',
    );

    foreach ($preload_fonts as $font) {
        printf(
            '<link rel="preload" href="%s" as="font" type="font/woff2" crossorigin>%s',
            esc_url({theme_slug}_asset_uri($font)),
            "\n"
        );
    }
    
    // DNS prefetch
    echo '<link rel="dns-prefetch" href="//api-maps.yandex.ru">' . "\n";
}
add_action('wp_head', '{theme_slug}_add_resource_hints', 1);
```

2. **Настроить ленивую загрузку изображений**:
```php
add_filter('wp_lazy_loading_enabled', '__return_true');
```

3. **Оптимизировать вывод скриптов (ОБНОВЛЕНО)**:
```php
// Используем wp_script_add_data вместо фильтра (более надежно)
wp_script_add_data('{theme_slug}-main', 'defer', true);
// Это добавляется сразу после wp_enqueue_script() в функции {theme_slug}_enqueue_scripts()
```

4. **Отключение ненужных стилей и скриптов WordPress (ОБНОВЛЕНО)**: ✅ **ДОБАВЛЕНО**
```php
/**
 * Отключение ненужных стилей и скриптов WordPress
 * Улучшает производительность и скрывает информацию о версии WordPress
 */
function {theme_slug}_disable_unnecessary_assets() {
    // Отключение стилей блоков Gutenberg (если не используются)
    wp_dequeue_style('wp-block-library');
    wp_dequeue_style('wp-block-library-theme');
    wp_dequeue_style('global-styles');
    wp_dequeue_style('classic-theme-styles');
    
    // Отключение эмодзи скриптов и стилей
    remove_action('wp_head', 'print_emoji_detection_script', 7);
    remove_action('admin_print_scripts', 'print_emoji_detection_script');
    remove_action('wp_print_styles', 'print_emoji_styles');
    remove_action('admin_print_styles', 'print_emoji_styles');
    remove_filter('the_content_feed', 'wp_staticize_emoji');
    remove_filter('comment_text_rss', 'wp_staticize_emoji');
    remove_filter('wp_mail', 'wp_staticize_emoji_for_email');
    add_filter('emoji_svg_url', '__return_false');
}
add_action('wp_enqueue_scripts', '{theme_slug}_disable_unnecessary_assets', 100);

/**
 * Удаление inline стилей эмодзи из head
 * Используем output buffering на уровне всей страницы
 */
function {theme_slug}_remove_emoji_inline_styles_start() {
    ob_start();
}
add_action('template_redirect', '{theme_slug}_remove_emoji_inline_styles_start', 1);

function {theme_slug}_remove_emoji_inline_styles_end() {
    $buffer = ob_get_clean();
    // Удаляем inline стили эмодзи
    $buffer = preg_replace('#<style id="wp-emoji-styles-inline-css">.*?</style>#s', '', $buffer);
    echo $buffer;
}
add_action('shutdown', '{theme_slug}_remove_emoji_inline_styles_end', 999);

/**
 * Скрытие версии WordPress
 */
function {theme_slug}_remove_wp_version() {
    return '';
}
add_filter('the_generator', '{theme_slug}_remove_wp_version');

/**
 * Удаление версии WordPress из query string стилей и скриптов
 */
function {theme_slug}_remove_version_scripts_styles($src) {
    if (strpos($src, 'ver=')) {
        $src = remove_query_arg('ver', $src);
    }
    return $src;
}
add_filter('style_loader_src', '{theme_slug}_remove_version_scripts_styles', 9999);
add_filter('script_loader_src', '{theme_slug}_remove_version_scripts_styles', 9999);
```

**Результат оптимизации:**
- Удаляются ненужные стили блоков Gutenberg (~50KB)
- Удаляются стили эмодзи и связанные скрипты (~20KB)
- Удаляются inline стили из head
- Скрывается версия WordPress в мета-теге generator
- Удаляется версия из URL стилей и скриптов (для безопасности)

### 7.2 Кэширование

**Задачи**:

1. **Добавить поддержку кэширования для Carbon Fields**:
```php
// В production использовать transients для кэширования
function {theme_slug}_get_cached_services() {
    $post_id = get_the_ID();
    $cache_key = '{theme_slug}_services_' . $post_id;
    $services = get_transient($cache_key);
    
    if (false === $services) {
        // ОБНОВЛЕНО: Используем helper-функцию
        $services = {theme_slug}_post_meta($post_id, 'services_items', array());
        set_transient($cache_key, $services, DAY_IN_SECONDS);
    }
    
    return $services;
}
```

2. **Сброс кэша при обновлении полей**:
```php
add_action('carbon_fields_post_meta_container_saved', '{theme_slug}_clear_cache');
function {theme_slug}_clear_cache($post_id) {
    delete_transient('{theme_slug}_services_' . $post_id);
    // Очищаем другие transients
}
```

---

## Фаза 8: Тестирование и отладка

### 8.1 Проверка загрузки шаблонов из подкаталога

**⚠️ КРИТИЧЕСКОЕ ПРОВЕРЕНИЕ:** Перед тестированием функциональности убедитесь, что шаблоны загружаются корректно.

**🔧 Использование переменных в коде:**

Во всех примерах кода данного документа используются следующие переменные-плейсхолдеры, которые **ОБЯЗАТЕЛЬНО** должны быть заменены на конкретные значения для вашего проекта:

| Переменная | Описание | Пример замены |
|-----------|----------|---------------|
| `{theme_slug}` | Slug темы (строчными буквами, без пробелов, дефисы допустимы) | `mytheme`, `business-site`, `corporate-theme` |
| `{THEME_NAME}` | Полное название темы | `My Theme`, `Business Theme`, `Corporate Theme` |
| `{THEME_URI}` | URL сайта темы или репозитория | `https://example.com`, `https://github.com/user/theme` |
| `{AUTHOR_NAME}` | Имя автора темы | `John Doe`, `Company Name` |
| `{THEME_DESCRIPTION}` | Описание темы | `Профессиональная тема для бизнес-сайта...` |

**⚠️ ВАЖНО:**
- Замените **ВСЕ** упоминания этих переменных в коде перед использованием
- Используйте **одинаковый** `{theme_slug}` во всех местах одного проекта
- Убедитесь, что `{theme_slug}` соответствует имени каталога темы

**Чек-лист проверки шаблонов**:

- [ ] **Шаблоны отображаются в админке:**
  - [ ] Откройте страницу "Главная страница" в редакторе
  - [ ] В настройках страницы (справа) должен быть выпадающий список "Шаблон страницы"
  - [ ] В списке должны быть шаблоны: "Главная страница", "Privacy Policy"
  - [ ] Если шаблоны не видны - проверьте фильтр `theme_page_templates` в `functions.php`

- [ ] **Шаблон выбран для страницы:**
  - [ ] Для главной страницы выбран шаблон "Главная страница"
  - [ ] Для страницы Privacy выбран шаблон "Privacy Policy"
  - [ ] Сохраните страницу после выбора шаблона

- [ ] **Шаблон загружается на фронтенде:**
  - [ ] Откройте главную страницу на фронтенде
  - [ ] Просмотрите исходный HTML (Ctrl+U)
  - [ ] Найдите комментарий: `<!-- DEBUG: Шаблон template-pages/page-home.php загружен! Post ID: ... -->`
  - [ ] Если комментария нет - шаблон не загружается, проверьте фильтры в `functions.php`

- [ ] **Секции загружаются:**
  - [ ] В исходном HTML найдите комментарий: `<!-- DEBUG: Hero section загружена! Post ID: ... -->`
  - [ ] Если комментария нет - проверьте наличие Carbon Fields и данных в админке

- [ ] **Диагностика проблем:**
  - [ ] Если шаблон не загружается - проверьте консоль браузера на ошибки
  - [ ] Проверьте, что все фильтры в `functions.php` зарегистрированы
  - [ ] Убедитесь, что путь к шаблону в базе данных корректен (`get_page_template_slug()`)
  - [ ] Очистите кеш браузера и WordPress (если используется кеширование)
  - [ ] Проверьте права доступа к файлам на сервере

**Временная отладка в шаблонах:**

Для диагностики добавьте временные DEBUG комментарии в начало шаблонов:

```php
<!-- DEBUG: Шаблон template-pages/page-home.php загружен! Post ID: <?php echo get_the_ID(); ?> -->
```

В секциях (например, `template-parts/home/hero-section.php`):

```php
<!-- DEBUG: Hero section загружена! Post ID: <?php echo $post_id; ?> -->
```

После подтверждения работы шаблонов эти комментарии можно удалить.

### 8.2 Функциональное тестирование

**Чек-лист общих функций**:

- [ ] Все секции отображаются корректно
- [ ] Модальные окна открываются/закрываются
- [ ] Мобильное меню работает
- [ ] Плавный скролл к якорям функционирует
- [ ] Яндекс карта загружается (или fallback на Google Maps)
- [ ] Темная/светлая тема переключается
- [ ] Анимации при скролле срабатывают
- [ ] Floating CTA панель показывается/скрываются

**Чек-лист тестирования форм и уведомлений**:

- [x] **Валидация формы работает корректно**: ✅ **ИСПРАВЛЕНО**
  - [x] Проверка пустых полей
  - [x] Проверка минимальной длины имени (2 символа)
  - [x] Проверка минимальной длины телефона (7 символов)
  - [x] Проверка согласия на обработку данных
  - [x] Показ понятных сообщений об ошибках
  - [x] **ВАЖНО**: Функция `submitForm` теперь правильно определена в каждом модуле (IIFE), использующем её

- [ ] **Маска телефона работает**:
  - [ ] Автоматическое форматирование при вводе
  - [ ] Корректная обработка вставки из буфера
  - [ ] Удаление лишних символов

- [x] **Telegram уведомления**: ✅ **ИСПРАВЛЕНО**
  - [x] Тестовое сообщение из админки отправляется
  - [x] Сообщения с реальных форм приходят в Telegram
  - [x] Формат сообщения соответствует статическому сайту (с эмодзи и форматированием)
  - [x] В сообщении присутствуют все данные: имя, телефон, сообщение, время, источник
  - [x] HTML форматирование применяется корректно
  - [x] Логирование работает (в режиме отладки)
  - [x] Обработка ошибок Telegram API (добавлена проверка поля `ok: true` в ответе API)
  - [x] **ВАЖНО**: Функция отправки правильно проверяет успешность ответа от Telegram API, а не только факт выполнения запроса

- [ ] **Email уведомления**:
  - [ ] Email приходят на указанный адрес (если настроено)
  - [ ] Формат письма корректен
  - [ ] Кодировка UTF-8 работает правильно
  - [ ] HTML в письме отображается

- [ ] **AJAX обработка**:
  - [ ] Nonce проверяется корректно
  - [ ] HTTP коды ответов правильные (200, 400, 403, 405, 500)
  - [ ] JSON ответы имеют правильную структуру
  - [ ] Заголовки кэширования установлены

- [ ] **Санитизация данных**:
  - [ ] Телефон очищается от лишних символов
  - [ ] Текст экранируется (защита от XSS)
  - [ ] Вредоносный HTML не проходит

**Сценарии тестирования Telegram интеграции**:

1. **Базовый тест**:
   - Зайти в "Настройки темы" → "Формы и Email"
   - Ввести Bot Token и Chat ID
   - Нажать "Отправить тестовое сообщение"
   - ✅ Проверить: тестовое сообщение пришло в Telegram чат

2. **Тест отправки формы с главной страницы**: ✅ **ИСПРАВЛЕНО**
   - Открыть модальное окно заказа
   - Заполнить: Имя, Телефон, Сообщение
   - Поставить галочку согласия
   - Нажать "Отправить заявку"
   - ✅ Проверить: сообщение пришло в Telegram со всеми данными
   - ✅ **ВАЖНО**: Теперь форма заказа (`orderForm`) корректно отправляет данные благодаря добавленной функции `submitForm` в соответствующем модуле

3. **Тест контактной формы**: ✅ **ИСПРАВЛЕНО**
   - Прокрутить до секции "Контакты"
   - Заполнить форму на странице
   - Отправить
   - ✅ Проверить: сообщение пришло в Telegram
   - ✅ **ВАЖНО**: Все формы теперь корректно обрабатываются благодаря правильной структуре модулей JavaScript

4. **Тест обработки ошибок**:
   - Ввести неверный Bot Token в настройках
   - Отправить форму
   - ✅ Проверить: пользователь видит сообщение об ошибке
   - ✅ Проверить: в логах есть информация об ошибке

5. **Тест логирования**:
   - Включить WP_DEBUG в wp-config.php
   - Отправить форму
   - ✅ Проверить: в wp-content/uploads/{theme_slug}-tg-log.txt есть записи
   - ✅ Проверить: в error_log есть записи о запросах/ответах

### 8.2 Тестирование Carbon Fields

**Чек-лист**:

- [ ] Все поля в админке отображаются
- [ ] Значения по умолчанию установлены
- [ ] Изменения сохраняются
- [ ] Данные выводятся на фронтенде
- [ ] Complex fields работают корректно
- [ ] Theme Options доступны глобально
- [ ] Условная логика полей работает (если есть)

### 8.3 Кросс-браузерное тестирование

**Тестировать в**:

- [ ] Chrome (последняя версия)
- [ ] Firefox (последняя версия)
- [ ] Safari (последняя версия)
- [ ] Edge (последняя версия)
- [ ] Мобильный Safari (iOS)
- [ ] Chrome Mobile (Android)

### 8.4 Адаптивность (Responsive Design)

**Тестировать на разрешениях**:

- [ ] 320px (мобильные)
- [ ] 768px (планшеты)
- [ ] 1024px (маленькие десктопы)
- [ ] 1920px+ (большие экраны)

### 8.5 Производительность

**Проверить**:

- [ ] Google PageSpeed Insights (>90 баллов)
- [ ] GTmetrix (Grade A)
- [ ] WebPageTest
- [ ] Lighthouse audit

**Оптимизации**:

- [ ] Сжатие изображений (WebP формат)
- [ ] Минификация CSS/JS
- [ ] Gzip/Brotli сжатие
- [ ] Browser caching
- [ ] CDN (опционально)

### 8.6 SEO проверка

**Чек-лист**:

- [ ] Правильные title теги
- [ ] Meta descriptions
- [ ] H1-H6 иерархия
- [ ] Alt теги для изображений
- [ ] Schema.org разметка валидна (проверить через Google Rich Results Test)
- [ ] Open Graph теги
- [ ] Twitter Cards
- [ ] XML Sitemap (через плагин)
- [ ] robots.txt корректен
- [ ] Canonical URLs
- [ ] 404 страница существует

### 8.7 Безопасность

**Чек-лист**:

- [ ] Nonce проверки в AJAX
- [ ] Санитизация всех входных данных
- [ ] Экранирование всего вывода
- [ ] SQL инъекции невозможны (используем WP функции)
- [ ] XSS атаки предотвращены
- [ ] CSRF защита работает
- [ ] Проверка прав доступа
- [ ] Безопасные заголовки HTTP

---

## Фаза 9: Документация и финализация

### 9.1 Создание README.md для темы

**Содержание**:

```markdown
# {THEME_NAME} WordPress Theme

## Описание
Профессиональная WordPress тема с полной интеграцией Carbon Fields для управления контентом и Telegram Bot для уведомлений о заявках.

## Требования
- WordPress 5.9+
- PHP 7.4+
- Carbon Fields 3.x
- SSL сертификат (рекомендуется)
- Telegram Bot (для уведомлений)

## Установка

1. Установите Carbon Fields через Composer:
   ```bash
   composer require htmlburger/carbon-fields
   ```

2. Загрузите тему в wp-content/themes/{theme_slug}

3. Активируйте тему в админке WordPress

4. Создайте новую страницу и выберите шаблон "Главная страница"

5. Настройте контент через метабоксы Carbon Fields

6. Настройте глобальные опции в "Настройки темы {THEME_NAME}"

## Структура контента

### Главная страница
Используйте шаблон "Главная страница" и заполните следующие секции:
- Hero Section
- Технические характеристики
- О компании
- Услуги
- Преимущества
- Как это работает
- Отзывы
- Контакты

### Страница Privacy Policy
Используйте шаблон "Privacy Policy" для страницы политики конфиденциальности. ВСЕ данные редактируются через Carbon Fields в разделе "Настройки страницы Privacy". Контент берется из `static_website/privacy.html`.

## Настройка уведомлений

### Настройка Telegram Bot (рекомендуется)

**Шаг 1: Создание бота**
1. Откройте Telegram и найдите @BotFather
2. Отправьте команду `/newbot`
3. Следуйте инструкциям:
   - Введите имя бота (например: "{Site Name} Notifications")
   - Введите username бота (должен заканчиваться на "bot", например: "{site_name}_notify_bot")
4. Скопируйте полученный токен (формат: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)

**Шаг 2: Получение Chat ID**
1. Создайте канал или группу в Telegram (или используйте существующую)
2. Добавьте вашего бота в канал/группу как администратора
3. Найдите @getmyid_bot в Telegram
4. Добавьте @getmyid_bot в ваш канал/группу
5. Скопируйте Chat ID (для групп и каналов начинается с минуса: `-1001234567890`)

**Шаг 3: Настройка в WordPress**
1. Перейдите в WordPress админку → "Настройки темы {THEME_NAME}"
2. Откройте вкладку "Формы и Email"
3. Вставьте Bot Token в поле "Telegram Bot Token"
4. Вставьте Chat ID в поле "Telegram Chat ID"
5. Нажмите "Сохранить изменения"
6. Нажмите кнопку "Отправить тестовое сообщение"
7. Проверьте, что тестовое сообщение пришло в ваш Telegram канал/группу

**Формат уведомлений Telegram:**
```
📋 НОВАЯ ЗАЯВКА С САЙТА

👤 Имя: Иван Иванов
📞 Телефон: +375291234567
📝 Сообщение: Нужно перевезти контейнер
⏰ Время: 31.10.2025 14:30:25
🌐 Источник: сайт
```

### Настройка Email уведомлений (опционально)

1. В разделе "Формы и Email" введите email для получения заявок
2. По умолчанию используется email администратора сайта
3. Настройте SMTP для надежной доставки (плагин WP Mail SMTP рекомендуется)

**Примечание:** Telegram является основным каналом уведомлений. Email отправляется дополнительно.

## Отладка и логирование

### Включение режима отладки

Добавьте в `wp-config.php`:
```php
define('WP_DEBUG', true);
define('WP_DEBUG_LOG', true);
define('WP_DEBUG_DISPLAY', false);
```

### Просмотр логов Telegram

Логи отправки в Telegram сохраняются в:
- `wp-content/uploads/{theme_slug}-tg-log.txt` - детальные логи запросов/ответов
- `wp-content/debug.log` - общий лог WordPress (если WP_DEBUG_LOG включен)

### Типовые проблемы

**Проблема:** Telegram сообщения не приходят  
**Решение:**
- Проверьте правильность Bot Token и Chat ID
- Убедитесь, что бот добавлен в канал/группу как администратор
- Проверьте логи в `{theme_slug}-tg-log.txt` (особенно поле "Успех: ДА/НЕТ")
- Используйте кнопку "Отправить тестовое сообщение" в админке
- **ВАЖНО**: Функция отправки теперь проверяет поле `ok: true` в ответе Telegram API, а не просто факт выполнения запроса
- Если в логах видите "API вернул ошибку" - проверьте формат Bot Token и Chat ID, а также права бота в канале/группе

**Проблема:** Формы не отправляются  
**Решение:**
- Проверьте консоль браузера на JavaScript ошибки (особенно ошибки о недоступных функциях)
- Убедитесь, что функция `submitForm` определена в каждом модуле (IIFE), который её использует
- Проверьте, что `{theme_slug}Data` объект загружен (должен быть доступен после `wp_localize_script`)
- Убедитесь, что AJAX URL корректен
- Проверьте, что nonce не устарел (очистите кэш)
- Для форм с ID, заканчивающимся на "Form" - убедитесь, что обработчик событий правильно подключен

**Проблема:** Контент главной страницы не отображается (пустая страница)  
**Решение:**
1. **Проверьте загрузку шаблона:**
   - Откройте страницу в админке → настройки страницы (справа) → проверьте, что выбран правильный шаблон
   - Если шаблона нет в списке - проверьте фильтр `theme_page_templates` в `functions.php`
   - Перевыберите шаблон и сохраните страницу

2. **Проверьте фильтры в `functions.php`:**
   - Убедитесь, что все 4 фильтра зарегистрированы (см. раздел 2.3)
   - Используйте функцию `get_template_directory()` для определения пути к теме

3. **Проверьте загрузку секций:**
   - Просмотрите исходный HTML страницы (Ctrl+U)
   - Найдите DEBUG комментарии для диагностики
   - Проверьте, что Carbon Fields активен и функции доступны

4. **Очистите кеш:**
   - Очистите кеш браузера (Ctrl+F5)
   - Очистите кеш WordPress (если используется плагин кеширования)
   - Проверьте, что `page.php` не используется вместо кастомного шаблона

5. **Проверьте условия Carbon Fields:**
   - Убедитесь, что условия в `carbon-fields-config.php` поддерживают разные варианты путей
   - Используйте `or_where()` для поддержки старых и новых форматов пути

6. **Проверьте права доступа:**
   - Убедитесь, что файлы шаблонов доступны для чтения
   - Проверьте логи ошибок PHP (если включен `WP_DEBUG`)

## Файловая структура

```
{theme_slug}/
├── assets/
│   ├── css/          # Стили
│   ├── fonts/        # Локальные шрифты
│   ├── images/       # Изображения
│   ├── icons/        # Иконки
│   └── js/           # JavaScript файлы
├── inc/
│   ├── carbon-fields-config.php  # Настройка Carbon Fields
│   ├── theme-setup.php           # Настройка темы
│   ├── enqueue-scripts.php       # Подключение стилей/скриптов
│   ├── form-helpers.php          # Функции санитизации форм
│   ├── telegram-handler.php      # Обработчик Telegram API
│   └── ajax-handlers.php         # AJAX обработчики
├── template-parts/   # Части шаблонов для секций
├── functions.php     # Основной файл функций
├── header.php        # Шапка сайта
├── footer.php        # Подвал сайта
├── index.php         # Главный шаблон
├── page.php          # Шаблон страницы
├── template-pages/   # Шаблоны страниц WordPress
│   ├── page-home.php     # Шаблон главной страницы
│   └── page-privacy.php  # Шаблон страницы Privacy Policy
├── style.css         # Основной файл стилей
└── screenshot.png    # Скриншот темы
```

## Безопасность

Тема реализует следующие меры безопасности:
- ✅ Nonce проверка для всех AJAX запросов
- ✅ Санитизация всех входящих данных
- ✅ Экранирование всего вывода
- ✅ Проверка прав доступа для admin функций
- ✅ Защита от XSS атак
- ✅ Защита от SQL инъекций (через WordPress API)
- ✅ HTTP коды ответов для различных ошибок

## Производительность

Оптимизации, реализованные в теме:
- Ленивая загрузка изображений
- Preload критических ресурсов
- DNS prefetch для внешних доменов
- Кэширование заголовков для AJAX
- Отложенная загрузка JavaScript
- WebP изображения
- Минимизация HTTP запросов
- **Отключение ненужных стилей WordPress** (Gutenberg, глобальные стили, классические стили) ✅ **ДОБАВЛЕНО**
- **Отключение эмодзи скриптов и стилей** ✅ **ДОБАВЛЕНО**
- **Скрытие версии WordPress** (безопасность) ✅ **ДОБАВЛЕНО**

## Поддержка

Для вопросов и поддержки:
- Email: {EMAIL_ДЛЯ_ПОДДЕРЖКИ}
- Telegram: {TELEGRAM_АККАУНТ} (если есть)

## Changelog

### Version 1.0.0 (2024-10-31)
- Первый релиз
- Полная интеграция Carbon Fields
- Telegram Bot интеграция
- Адаптивный дизайн
- Темная/светлая тема
- SEO оптимизация
```

### 9.2 Создание документации для редактора контента

**Задачи**:

1. **Создать `docs/content-editor-guide.md`**:
   - Инструкции по редактированию каждой секции
   - Скриншоты интерфейса Carbon Fields
   - Рекомендации по оптимальному размеру изображений
   - Советы по SEO оптимизации контента

2. **Создать `docs/troubleshooting.md`**:
   - Часто встречающиеся проблемы
   - Решения типовых ошибок
   - Контакты для поддержки

### 9.3 Code Review и рефакторинг

**Чек-лист**:

- [ ] Код соответствует WordPress Coding Standards
- [ ] Все функции имеют PHPDoc комментарии
- [ ] Нет дублирования кода
- [ ] Нет hardcoded значений (все через Carbon Fields или настройки)
- [ ] Все строки подготовлены для переводов (i18n)
- [ ] Нет PHP ошибок и warnings
- [ ] JavaScript без ошибок в консоли
- [ ] CSS валиден

### 9.4 Финальная проверка перед деплоем

**Чек-лист**:

- [ ] Все изображения оптимизированы
- [ ] Все зависимости установлены
- [ ] .gitignore настроен корректно
- [ ] Debug mode отключен в production
- [ ] Все credentials в безопасности (не в репозитории)
- [ ] Резервная копия создана
- [ ] Staging тестирование пройдено
- [ ] Google Analytics подключен
- [ ] Формы тестированы в реальных условиях
- [ ] Email уведомления тестированы
- [ ] SSL сертификат установлен
- [ ] CDN настроен (если используется)
- [ ] Кэширование настроено

---

## Фаза 10: Деплой и пост-релизные задачи

### 10.1 Подготовка к деплою

**Задачи**:

1. **Создать production версию темы**:
```bash
# Минификация CSS
npm run build:css

# Минификация JS
npm run build:js

# Оптимизация изображений
npm run optimize:images
```

2. **Создать архив темы для установки**:
```bash
zip -r {theme_slug}-v1.0.0.zip {theme_slug}/ -x "*.git*" "node_modules/*" "*.md"
```

### 10.2 Процесс деплоя

**Шаги**:

1. [ ] Создать полную резервную копию текущего сайта
2. [ ] Загрузить тему на сервер
3. [ ] Активировать тему
4. [ ] Импортировать начальные данные (если есть)
5. [ ] Настроить все Carbon Fields опции
6. [ ] Протестировать все функции
7. [ ] Проверить формы и email
8. [ ] Провести финальное SEO тестирование

### 10.3 Пост-релизный мониторинг

**Задачи на первую неделю**:

- [ ] Мониторинг ошибок в логах
- [ ] Проверка отправки форм
- [ ] Анализ Google Analytics
- [ ] Проверка индексации в Google Search Console
- [ ] Мониторинг производительности
- [ ] Сбор обратной связи от пользователей

### 10.4 План обслуживания

**Регулярные задачи**:

- **Еженедельно**:
  - Проверка работы форм
  - Проверка резервных копий
  - Мониторинг производительности

- **Ежемесячно**:
  - Обновление WordPress core
  - Обновление плагинов
  - Обновление темы (если есть изменения)
  - SEO аудит

- **Ежеквартально**:
  - Полный аудит безопасности
  - Оптимизация базы данных
  - Ревью аналитики и конверсий

---

## Временные оценки

| Фаза | Описание | Время |
|------|----------|-------|
| 1 | Подготовка и структура темы | 4 часа |
| 2 | Настройка WordPress функций | 3 часа |
| 3 | Интеграция Carbon Fields | 8 часов |
| 4 | Создание шаблонов | 6 часов |
| 5 | Обработка форм, AJAX и Telegram | 6 часов |
| 6 | SEO и Schema.org | 3 часа |
| 7 | Оптимизация производительности | 3 часа |
| 8 | Тестирование (включая Telegram) | 7 часов |
| 9 | Документация | 3 часа |
| 10 | Деплой и пост-релиз | 2 часа |
| **ИТОГО** | | **45 часов** |

**Детализация Фазы 5 (Telegram интеграция)**:
- Создание функций санитизации (form-helpers.php) - 1 час
- Портирование Telegram handler (telegram-handler.php) - 1.5 часа
- AJAX обработчик с логикой из send-form.php - 1.5 часа
- Модификация JavaScript - 1 час
- Настройка админки с тестированием Telegram - 1 час

*Примечание: Временные оценки являются приблизительными и могут варьироваться в зависимости от опыта разработчика и непредвиденных сложностей.*

---

## Риски и их минимизация

### Риск 1: Проблемы с совместимостью Carbon Fields
**Вероятность**: Низкая  
**Влияние**: Высокое  
**Минимизация**: 
- Использовать последнюю стабильную версию Carbon Fields
- Тщательно тестировать все поля после создания
- Иметь fallback значения для всех полей

### Риск 2: Проблемы с производительностью из-за большого количества полей
**Вероятность**: Средняя  
**Влияние**: Среднее  
**Минимизация**:
- Использовать кэширование (transients)
- Оптимизировать запросы к базе данных
- Использовать lazy loading для изображений

### Риск 3: Ошибки при миграции JavaScript функциональности
**Вероятность**: Средняя  
**Влияние**: Среднее  
**Минимизация**:
- Тщательное тестирование всех JS функций
- Использовать console.log для отладки
- Постепенная миграция с тестированием каждого модуля

### Риск 4: SEO потери при переходе на WordPress
**Вероятность**: Низкая  
**Влияние**: Высокое  
**Минимизация**:
- Сохранить все URL структуры
- Правильно настроить редиректы
- Сохранить всю мета-информацию
- Проверить через Google Search Console

---

## Дополнительные рекомендации

### Плагины для улучшения функциональности

**Рекомендуемые**:
- **Yoast SEO** или **Rank Math** - расширенная SEO оптимизация
- **WP Rocket** - кэширование и оптимизация производительности
- **Wordfence Security** - безопасность сайта
- **UpdraftPlus** - автоматические резервные копии
- **WP Mail SMTP** - надежная отправка email
- **Contact Form 7** - дополнительные формы (если потребуется)

**Опциональные**:
- **WPML** - мультиязычность (если планируется)
- **Redis Object Cache** - кэширование объектов
- **Smush** - оптимизация изображений
- **WP Super Cache** - альтернатива WP Rocket

### Советы по поддержке и развитию

1. **Версионирование темы**:
   - Используйте семантическое версионирование (1.0.0, 1.1.0, 2.0.0)
   - Ведите CHANGELOG.md с описанием изменений

2. **Git workflow**:
   - Создайте репозиторий для темы
   - Используйте ветки для разработки новых функций
   - Делайте регулярные коммиты с понятными сообщениями

3. **Staging окружение**:
   - Всегда тестируйте изменения на staging перед production
   - Используйте идентичную конфигурацию

4. **Мониторинг**:
   - Настройте уведомления об ошибках
   - Используйте Google Analytics для отслеживания поведения пользователей
   - Настройте uptime мониторинг

---

## Контрольный список готовности к внедрению

### Разработка
- [ ] Все файлы темы созданы
- [ ] Carbon Fields настроен и работает
- [ ] Все секции интегрированы
- [ ] Формы работают
- [ ] JavaScript функционал перенесен
- [ ] Стили применяются корректно

### Контент
- [ ] Главная страница заполнена
- [ ] Страница Privacy создана
- [ ] Все изображения загружены
- [ ] Мета-данные заполнены
- [ ] Меню настроены

### Технические аспекты
- [ ] SEO настроен
- [ ] Schema.org разметка добавлена
- [ ] Google Analytics подключен
- [ ] Формы отправляют email
- [ ] Карта отображается
- [ ] SSL сертификат установлен
- [ ] Производительность оптимизирована

### Тестирование
- [ ] Функциональное тестирование пройдено
- [ ] Кросс-браузерное тестирование пройдено
- [ ] Адаптивность проверена
- [ ] SEO проверка выполнена
- [ ] Безопасность проверена
- [ ] Производительность проверена

### Документация
- [ ] README.md создан
- [ ] Документация для редактора контента готова
- [ ] Troubleshooting guide подготовлен

### Деплой
- [ ] Резервная копия создана
- [ ] Тема загружена на production
- [ ] Финальное тестирование выполнено
- [ ] Мониторинг настроен

---

## Заключение

Данный план предоставляет пошаговое руководство по трансформации статического HTML/CSS/JS сайта в WordPress тему с использованием Carbon Fields и Telegram Bot API. План учитывает все аспекты разработки: от базовой структуры темы до деплоя и пост-релизного мониторинга.

Следуя этому плану, вы создадите профессиональную, производительную и легко управляемую WordPress тему с следующими ключевыми возможностями:

### ✅ Основные возможности

1. **Управление контентом через Carbon Fields**
   - Интуитивный интерфейс для редактирования всех секций сайта
   - Повторяющиеся поля для услуг, отзывов, характеристик
   - Глобальные настройки темы
   - Возможность добавления/удаления элементов без изменения кода

2. **Telegram Bot интеграция**
   - Мгновенные уведомления о новых заявках в Telegram
   - Портированная логика из статического сайта
   - Форматирование с эмодзи и HTML
   - Тестирование подключения прямо из админки
   - Детальное логирование для отладки
   - Функции санитизации из оригинального кода

3. **Обработка форм** ✅ **ИСПРАВЛЕНО**
   - Валидация на клиенте и сервере
   - Маска телефонного номера
   - AJAX отправка без перезагрузки страницы
   - Защита от спама и атак
   - Дублирование на Email (опционально)
   - **ВАЖНО**: Исправлена проблема с отправкой форм заказа - функция `submitForm` теперь правильно определена в каждом модуле (IIFE)
   - **ВАЖНО**: Улучшена проверка успешности отправки в Telegram - теперь проверяется поле `ok: true` в ответе API

4. **SEO и производительность**
   - Schema.org разметка
   - Open Graph и Twitter Cards
   - Оптимизированная загрузка ресурсов
   - Адаптивные изображения
   - Кэширование

5. **Современный UI/UX**
   - Темная/светлая тема
   - Плавные анимации
   - Адаптивный дизайн
   - Доступность (ARIA атрибуты)

### 📊 Реализация

**Общий срок реализации**: 5-7 рабочих дней (при работе full-time)

**Приоритеты реализации**:
1. **Фазы 1-4** (критический путь) - структура и шаблоны
2. **Фаза 5** (формы и Telegram) - ключевая функциональность
3. **Фазы 6-7** (SEO и оптимизация) - улучшения
4. **Фазы 8-10** (тестирование и деплой) - финализация

### 🎯 Результат

В итоге вы получите полностью функциональную WordPress тему, которая:
- Легко редактируется через админку без знания кода
- Мгновенно уведомляет о заявках через Telegram
- Сохраняет весь функционал статического сайта
- Готова к дальнейшему расширению и поддержке

Удачи в реализации проекта! 🚀

